# C1A — remise d'aplomb et refonte : plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Réparer le mapping AcroForm du formulaire ONEM C1A (les données s'impriment aujourd'hui dans les mauvaises cases), puis reconstruire son parcours à partir de l'arbre de renvois imprimé sur le document officiel.

**Architecture:** Deux lots séquentiels. Le lot 1 corrige la correspondance champ→widget en s'appuyant sur les coordonnées réelles du PDF, et installe un test de géométrie qui rend la classe de bug détectable sur les huit formulaires. Le lot 2 transcrit l'arbre des « voir N » en table déclarative, dont un compilateur pur dérive les conditions d'affichage, puis habille le formulaire (macro-étapes, aides sourcées, mobile).

**Tech Stack:** TypeScript 5 strict · vitest 4 · pdf-lib (via `lib/pdf-forms/acroform-parser`) · Next.js 16 · Prisma 5 (Neon partagée).

## Global Constraints

- **Spec de référence** : `docs/superpowers/specs/2026-07-28-c1a-refonte-design.md`. Toute divergence doit être signalée, pas décidée seule.
- **Jamais `prisma db push`** sur Neon (base partagée) → SQL additif via `prisma db execute`. Aucune migration n'est requise par ce plan.
- **`git add` de chemins EXPLICITES uniquement** — le workdir est partagé entre plusieurs agents. Jamais `git add -A`, jamais `--force` sur `main`.
- **ESLint est déjà rouge** (~124 erreurs préexistantes) : ne pas en ajouter, ne pas chercher à les corriger.
- **Aucun texte d'aide inventé.** Tout libellé ou explication affiché au citoyen est recopié du PDF officiel `private/pdfs/C1A_FR.pdf`. En cas de doute sur une formulation métier, poser la question plutôt que rédiger.
- **Ne jamais masquer (`hidden: true`) un widget unique porteur de donnée** — le filler saute les champs masqués (`lib/pdf-forms/filler.ts:913`), la case officielle partirait blanche.
- **Le runner lit la DB, pas le code.** Après toute modification de seed, re-semer via `pnpm tsx scripts/apply-c1-improvements.ts --yes` avant toute vérification à l'écran.
- Commandes de validation : `pnpm test` · `pnpm build` (fait le typecheck ; il n'existe pas de `pnpm typecheck`) · `pnpm lint` · `pnpm i18n:check`.
- Le repo est en français : commentaires, libellés, messages de commit.

## Coordonnées de référence (mesurées sur `private/pdfs/C1A_FR.pdf`)

Origine bas-gauche, points PDF. Page A4 595 × 842. Mise en page à **deux colonnes**, séparation à x ≈ 300.

| Page | Widget | x | y | Ce que c'est réellement |
|---|---|---|---|---|
| 1 | `Nom` | 43 | 359 | Q2 — nom de l'indépendant aidé |
| 1 | `TVA` (enfant 1) | 113 | 342 | Q2 — n° d'entreprise de l'indépendant aidé |
| 2 | `TVA` (enfant 2) | 117 | 302 | Q16 — n° d'entreprise de l'activité |
| 1 | `Adresse de lactivité indépendante` | 43 | 308 | Q2 — rue + numéro de l'activité |
| 1 | `rue_2` | 43 | 284 | **Q2 — code postal + commune** |
| 2 | `14 Données concernant votre employeur` | 49 | 500 | **Q14 — nom de l'employeur** |
| 2 | `Nom employeur` | 49 | 477 | **Q14 — adresse de l'employeur** |
| 2 | `A quelle adresse exercezvous cette activité` | 49 | 421 | Q15 — rue + numéro |
| 2 | `rue_3` | 49 | 398 | **Q15 — code postal + commune** |
| 2 | `personne phys` | 48 | 344 | Q16 — option personne physique |
| 2 | `mandataire administrateur ou gestionnaire` | 48 | 331 | Q16 — option mandataire |
| 2 | `non_7` / `toggle_8` | 48 / 92 | 301 | Q16 — « je dispose d'un n° d'entreprise » non / oui |
| 2 | `undefined_2` | 126 | 287 | **Q16 — 1ʳᵉ ligne « je décris mon activité »** |
| 2 | `Je décris mon activité 1` | 49 | 274 | Q16 — 2ᵉ ligne |
| 2 | `Je décris mon activité 2` | 49 | 261 | Q16 — 3ᵉ ligne |
| 2 | `voir 19` (enfant 3) | 358 | 559 | **Q19 — revenu net salarié par mois** |
| 2 | `voir 19` (enfant 4) | 485 | 561 | **Q19 — revenu net salarié par heure** |
| 2 | `voir 19` (enfant 2) | 348 | 490 | **Q19 — revenu net indépendant par an** |
| 2 | `voir 19` (enfant 1) | 335 | 647 | Q18 — 3ᵉ ligne « irrégulièrement » |
| 1 | `Montant` (enfant 1) | 318 | 308 | Q6 — montant de l'aide |
| 2 | `Montant` (enfant 2) | 67 | 707 | **Q11 — 1ᵉʳ montant du mandat** |
| 2 | `Montant` (enfant 3) | 67 | 695 | **Q11 — 2ᵉ montant du mandat** |
| 2 | `Liste déroulante44` | 350 | 171 | **Q24 — nombre d'annexes jointes** |
| 2 | *(aucun widget)* | ≈49 | 743→779 | Q10 — trois lignes « quel mandat » |

**Règle de nommage de ce PDF** : chaque widget porte le nom du texte imprimé
**juste au-dessus de lui**, pas celui de la donnée qu'il reçoit. C'est la cause
racine du décalage d'une ligne.

**Trois champs AcroForm portent plusieurs widgets partageant une seule valeur.**
Écrire dans l'un les remplit tous — ils sont donc inutilisables dès que deux
emplacements attendent des valeurs différentes. Vérifié via `pypdf` (`/Kids`) :

| Champ | Enfants | Emplacements couverts |
|---|---|---|
| `TVA` | 2 | n° d'entreprise de **Q2** (p1) **et** de **Q16** (p2) |
| `Montant` | 3 | montant de **Q6** (p1) **et** les deux montants de **Q11** (p2) |
| `voir 19` | 4 | une ligne de **Q18** **et** les trois revenus de **Q19** |

Conséquence : les sept valeurs concernées passent toutes en écriture
positionnelle, et ces trois champs restent non revendiqués.

## File Structure

| Fichier | Responsabilité | Lot |
|---|---|---|
| `lib/pdf-forms/__tests__/widget-geometry.test.ts` | *(créé)* Test de géométrie : l'ordre déclaré des champs doit suivre l'ordre de lecture du PDF | 1 |
| `lib/pdf-forms/seed/c1a-fields.ts` | *(modifié)* Schéma du C1A — réalignement, `drawAt`, arbre, aides | 1 + 2 |
| `lib/pdf-forms/seed/__tests__/c1a-fields.test.ts` | *(modifié)* Assertions du schéma C1A | 1 + 2 |
| `lib/pdf-forms/seed/_shared/moules.ts` | *(créé)* Moules partagés : sections, `YN`, `ouiNon` paramétré | 2 |
| `lib/pdf-forms/seed/c1/helpers.ts` | *(modifié)* Devient une façade qui réexporte depuis `_shared` | 2 |
| `lib/pdf-forms/routing.ts` | *(créé)* Compilateur d'arbre : table de renvois → `visibleIf` | 2 |
| `lib/pdf-forms/__tests__/routing.test.ts` | *(créé)* Tests du compilateur | 2 |
| `lib/pdf-forms/seed/c1a-routing.ts` | *(créé)* La table des « voir N » du C1A | 2 |
| `lib/pdf-forms/form-presentation.ts` | *(modifié)* Entrée `c1a` : ordre et titres des macro-étapes | 2 |
| `lib/pdf-forms/section-labels.ts` | *(modifié)* Deux libellés manquants | 2 |
| `messages/fr.json` | *(modifié)* Titres et descriptions des 5 étapes du C1A | 2 |

---

# LOT 1 — remettre les données dans les bonnes cases

### Task 1 : Test de géométrie — l'ordre déclaré doit suivre l'ordre de lecture

**Pourquoi ce test.** Le défaut du C1A n'est pas une faute de frappe : c'est un
décalage systématique invisible aux tests actuels, qui vérifient seulement qu'un
nom de widget existe. Un formulaire papier se lit dans un ordre ; les champs du
schéma sont numérotés (`order`) dans ce même ordre. Si le champ n°63 pointe vers
une case située **plus bas** que celle du champ n°65, quelque chose est faux.
C'est mécanique, ça ne demande aucune connaissance du métier, et ça se vérifie
sur les huit formulaires.

Le test tient compte des **deux colonnes** : on lit la colonne de gauche de haut
en bas, puis celle de droite.

**Files:**
- Create: `lib/pdf-forms/__tests__/widget-geometry.test.ts`

**Interfaces:**
- Consumes: `parsePdf` (`lib/pdf-forms/acroform-parser.ts`), les 8 fonctions `applyXImprovements` des seeds, `AcroFieldRaw` (`page?: number`, `rect?: [x, y, w, h]`).
- Produces: rien (test terminal). Le tableau `ECARTS_ASSUMES` qu'il définit sera étendu par les tâches suivantes.

- [ ] **Step 1 : Écrire le test qui échoue**

Créer `lib/pdf-forms/__tests__/widget-geometry.test.ts` :

```ts
import { describe, it, expect } from "vitest";
import { readFileSync, existsSync } from "node:fs";
import { join } from "node:path";
import { parsePdf } from "../acroform-parser";
import type { AcroFieldRaw, PdfFormField } from "../types";

import { applyC1Improvements } from "../seed/c1-fields-improvements";
import { applyC1RegisImprovements } from "../seed/c1-regis-fields";
import { applyC1PartenaireImprovements } from "../seed/c1-partenaire-fields";
import { applyC1AImprovements } from "../seed/c1a-fields";
import { applyC1BImprovements } from "../seed/c1b-fields";
import { applyC1CImprovements } from "../seed/c1c-fields";
import { applyC46Improvements } from "../seed/c46-fields";
import { applyC47Improvements } from "../seed/c47-fields";

/// Géométrie seed ↔ PDF réel.
///
/// Dans les AcroForms de l'ONEM, un widget porte le nom du texte imprimé JUSTE
/// AU-DESSUS de lui, pas celui de la donnée qu'il reçoit : la case
/// « Nom employeur » du C1A est la ligne de l'ADRESSE. Associer les champs aux
/// widgets par ressemblance de libellé produit donc un décalage d'une ligne,
/// que `seeds-vs-pdf` ne voit pas (il vérifie qu'un nom existe, jamais qu'il
/// désigne la bonne case).
///
/// Invariant vérifié ici : un formulaire papier se lit dans un ordre, et les
/// champs du schéma sont numérotés dans ce même ordre. Si le champ n°63 pointe
/// vers une case plus BASSE que celle du champ n°65, la correspondance est
/// fausse. Aucune connaissance métier requise — c'est de la géométrie.
///
/// Mise en page à deux colonnes : on lit la gauche de haut en bas, puis la
/// droite. L'ancre de tri est donc (page, colonne, -y).

const PDF_DIR = join(process.cwd(), "private", "pdfs");

/// Abscisse de séparation des colonnes, en points. A4 = 595 pt de large.
const COLONNE_X = 300;

/// Tolérance verticale, en points. Deux widgets d'une même ligne imprimée ne
/// sont pas exactement à la même hauteur (une case à cocher est plus petite
/// qu'une ligne de texte) : sans marge, une ligne « lundi ☐ ☐ ☐ » lèverait une
/// fausse alerte.
const TOLERANCE_Y = 12;

interface Cible {
  slug: string;
  pdf: string;
  improve: (fields: PdfFormField[], ctx?: { technicalSchema: AcroFieldRaw[] }) => PdfFormField[];
}

const CIBLES: Cible[] = [
  {
    slug: "c1-changement-situation",
    pdf: "C1_FR.pdf",
    improve: (fields, ctx) =>
      applyC1Improvements(fields, {
        defaultMotif: "modification",
        restrictMotifTo5Situations: true,
        technicalSchema: ctx?.technicalSchema,
      }),
  },
  { slug: "c1-regis", pdf: "Annexe_Regis_FR.pdf", improve: applyC1RegisImprovements },
  { slug: "c1-partenaire", pdf: "C1-Partenaire_FR.pdf", improve: applyC1PartenaireImprovements },
  { slug: "c1a", pdf: "C1A_FR.pdf", improve: applyC1AImprovements },
  { slug: "c1b", pdf: "C1B_FR.pdf", improve: applyC1BImprovements },
  { slug: "c1c", pdf: "C1C_FR.pdf", improve: applyC1CImprovements },
  { slug: "c46", pdf: "C46_FR.pdf", improve: applyC46Improvements },
  { slug: "c47", pdf: "C47_FR.pdf", improve: applyC47Improvements },
];

/// Écarts connus et assumés, par slug : `"champA > champB"` signifie que le
/// champ A est déclaré AVANT B mais pointe vers une case située après lui dans
/// l'ordre de lecture.
///
/// Une entrée ici est une DETTE, pas une exemption : elle documente un
/// formulaire dont le mapping n'a pas encore été réaligné. Les six compagnons
/// autres que le C1A ont été écrits avec la même méthode et présentent
/// probablement le même défaut ; leurs écarts sont consignés ici pour être
/// traités formulaire par formulaire, hors de ce lot.
const ECARTS_ASSUMES: Record<string, string[]> = {
  // À remplir à l'exécution du Step 2, sauf pour c1a qui doit finir à [].
};

interface Ancre {
  id: string;
  order: number;
  page: number;
  colonne: number;
  y: number;
}

/// Ancre géométrique d'un champ : la position de son widget. `null` si le champ
/// n'a pas de widget exploitable (champ virtuel, `drawAt`, widget absent du
/// PDF, ou masqué — un champ masqué n'est jamais stampé).
function ancre(field: PdfFormField, widgets: Map<string, AcroFieldRaw>): Ancre | null {
  if (field.hidden) return null;
  if (!field.pdfFieldName) return null;
  // Un `pdfFieldName` en "a|b|c" désigne N cases (radio sur N cases distinctes) :
  // on s'ancre sur la première, les autres sont adjacentes.
  const premier = field.pdfFieldName.split("|")[0];
  if (!premier) return null;
  const w = widgets.get(premier);
  if (!w || w.page === undefined || !w.rect) return null;
  const [x, y] = w.rect;
  return {
    id: field.id,
    order: field.order ?? 0,
    page: w.page,
    colonne: x < COLONNE_X ? 0 : 1,
    y,
  };
}

/// Vrai si `b` vient après `a` dans l'ordre de lecture du document.
function suitEnLecture(a: Ancre, b: Ancre): boolean {
  if (b.page !== a.page) return b.page > a.page;
  if (b.colonne !== a.colonne) return b.colonne > a.colonne;
  // Même colonne : on descend, donc y diminue.
  return b.y <= a.y + TOLERANCE_Y;
}

describe("géométrie seed ↔ PDF — l'ordre déclaré suit l'ordre de lecture", () => {
  for (const cible of CIBLES) {
    const path = join(PDF_DIR, cible.pdf);

    it(`${cible.slug} (${cible.pdf})`, async ({ skip }) => {
      if (!existsSync(path)) skip();

      const parsed = await parsePdf(readFileSync(path));
      const widgets = new Map(parsed.fields.map((f) => [f.pdfFieldName, f]));
      const fields = cible.improve([], { technicalSchema: parsed.fields });

      const ancres = fields
        .map((f) => ancre(f, widgets))
        .filter((a): a is Ancre => a !== null)
        .sort((a, b) => a.order - b.order);

      const ecarts: string[] = [];
      for (let i = 1; i < ancres.length; i++) {
        const precedent = ancres[i - 1];
        const courant = ancres[i];
        if (!suitEnLecture(precedent, courant)) {
          ecarts.push(`${precedent.id} > ${courant.id}`);
        }
      }

      expect(
        ecarts.sort(),
        `${cible.slug} : champs dont la case remonte dans le document`,
      ).toEqual((ECARTS_ASSUMES[cible.slug] ?? []).slice().sort());
    });
  }
});
```

- [ ] **Step 2 : Lancer le test et relever les écarts réels**

Run: `pnpm vitest run lib/pdf-forms/__tests__/widget-geometry.test.ts`

Expected: **ÉCHEC sur plusieurs formulaires**, dont `c1a`. Le message d'erreur
liste les paires `champA > champB`.

Relever la liste exacte produite pour **chaque** slug. Reporter ces listes dans
`ECARTS_ASSUMES` **sauf pour `c1a`**, qu'on laisse à `[]` : c'est le formulaire
que ce lot répare, son test doit rester rouge jusqu'à la Task 2.

Exemple de forme attendue (les valeurs réelles proviennent de l'exécution) :

```ts
const ECARTS_ASSUMES: Record<string, string[]> = {
  // Dette : ces formulaires ont été écrits avec la même méthode que le C1A et
  // n'ont pas encore été réalignés. Un lot par formulaire, hors de celui-ci.
  c1b: ["champX > champY"],
  c47: ["champZ > champW"],
};
```

Si un slug ne produit **aucun** écart, ne pas créer d'entrée pour lui.

- [ ] **Step 3 : Relancer et vérifier que seul `c1a` échoue**

Run: `pnpm vitest run lib/pdf-forms/__tests__/widget-geometry.test.ts`

Expected: 7 tests PASS, `c1a` FAIL avec une liste d'écarts non vide.

- [ ] **Step 4 : Commit**

```bash
git add lib/pdf-forms/__tests__/widget-geometry.test.ts
git commit -m "test(pdf): la geometrie des widgets doit suivre l'ordre de lecture

Les tests actuels verifient qu'un pdfFieldName existe dans le PDF, jamais qu'il
designe la bonne case. Or dans les AcroForms ONEM un widget porte le nom du
texte imprime JUSTE AU-DESSUS de lui : la case << Nom employeur >> du C1A est la
ligne de l'ADRESSE. Associer les champs par ressemblance de libelle decale donc
tout d'une ligne, en silence.

Ce test compare l'ordre declare des champs a la position reelle de leur widget,
en tenant compte des deux colonnes. Il echoue sur c1a, repare par la tache
suivante ; les ecarts des autres formulaires sont consignes comme dette."
```

---

### Task 2 : Réaligner Q2, Q14, Q15 et Q16

**Files:**
- Modify: `lib/pdf-forms/seed/c1a-fields.ts` (Q2 : ~lignes 346-383 · Q14-Q16 : ~lignes 648-755)
- Modify: `lib/pdf-forms/__tests__/widget-geometry.test.ts` (retirer `c1a` de la dette s'il y a été ajouté par erreur)

**Interfaces:**
- Consumes: le test de la Task 1.
- Produces: le champ `independantNumeroEntreprise` (type `bce`), sur lequel la Task 3 du lot 3 (anti-doublon) s'appuiera.

- [ ] **Step 1 : Réaligner le bloc Q14 (employeur)**

Dans `lib/pdf-forms/seed/c1a-fields.ts`, remplacer le champ `employeurNom`, le
commentaire qui le suit, et le champ `employeurAdresse` par :

```ts
  {
    // Le widget porte le nom du TITRE imprimé au-dessus de lui ("14. Données
    // concernant votre employeur"), mais il est posé sur la 1ʳᵉ ligne de
    // saisie — celle légendée « nom » juste en dessous (widget y=500, ligne
    // imprimée y=507, légende y=498). C'est bien la case du NOM.
    //
    // Un commentaire antérieur le tenait pour un intitulé de zone et le
    // laissait non référencé : la ligne du nom partait donc vide à l'ONEM.
    id: "employeurNom",
    pdfFieldName: "14 Données concernant votre employeur",
    type: "text",
    required: false,
    label: { fr: "14. Nom de votre employeur" },
    visibleIf: { fieldId: "activiteCommeSalarie", op: "equals", value: "oui" },
    section: SECTION_EMPLOYEUR,
    order: 63,
  },
  {
    // Même décalage : le widget nommé « Nom employeur » est posé sur la 2ᵉ
    // ligne, légendée « adresse » (widget y=477, ligne imprimée y=484).
    id: "employeurAdresse",
    pdfFieldName: "Nom employeur",
    type: "text",
    required: false,
    label: { fr: "Adresse de votre employeur" },
    visibleIf: { fieldId: "activiteCommeSalarie", op: "equals", value: "oui" },
    section: SECTION_EMPLOYEUR,
    order: 64,
  },
```

- [ ] **Step 2 : Réaligner le bloc Q15 (adresse de l'activité)**

Remplacer le champ `adresseActiviteNumero` par :

```ts
  {
    // `rue_3` est la 2ᵉ ligne de Q15, légendée « code postal commune »
    // (widget y=398, ligne imprimée y=405). Elle était affectée à l'adresse de
    // l'employeur (Q14), et ce champ-ci écrivait dans `undefined_2`, qui est en
    // réalité la 1ʳᵉ ligne de description d'activité de Q16.
    id: "adresseActiviteCodePostalCommune",
    legacyIds: ["adresseActiviteNumero"],
    pdfFieldName: "rue_3",
    type: "text",
    required: false,
    label: { fr: "Code postal et commune (activité accessoire)" },
    visibleIf: { fieldId: "autreActiviteAccessoire", op: "equals", value: "oui" },
    section: SECTION_ADRESSE,
    order: 66,
  },
```

- [ ] **Step 3 : Réaligner le bloc Q16 (descriptions) et libérer `TVA`**

Remplacer le champ `numeroEntreprise` et les deux `descriptionActivite*` par :

```ts
  {
    // Le champ `TVA` porte DEUX widgets : la case n° d'entreprise de Q2 (p1,
    // y=342, celle de l'indépendant aidé) et celle de Q16 (p2, y=302, celle de
    // l'activité). Ils partagent une seule valeur — écrire dans l'un remplit
    // l'autre. Ce champ écrivait donc le n° de l'activité DANS LES DEUX cases,
    // y compris celle de l'indépendant qu'on aide.
    //
    // Aucune réattribution ne peut résoudre ça : les deux questions passent en
    // écriture positionnelle et `TVA` reste non revendiqué.
    id: "numeroEntreprise",
    pdfFieldName: "",
    drawAt: { page: 1, x: 119, y: 298, size: 9, maxWidth: 155 },
    type: "bce",
    required: false,
    label: { fr: "Numéro d'entreprise (BCE)" },
    visibleIf: { fieldId: "disposeNumeroEntreprise", op: "equals", value: "oui" },
    section: SECTION_ACTIVITES,
    order: 69,
  },
  {
    // « Je décris mon activité » compte TROIS lignes. La première est le widget
    // `undefined_2` (y=287), posé sur la ligne qui prolonge le libellé ; les
    // deux suivantes sont `Je décris mon activité 1` et `2`. Les descriptions
    // étaient décalées d'un cran et la 1ʳᵉ ligne servait au code postal de Q15.
    id: "descriptionActivite1",
    pdfFieldName: "undefined_2",
    type: "text",
    required: false,
    label: { fr: "Je décris mon activité" },
    visibleIf: { fieldId: "autreActiviteAccessoire", op: "equals", value: "oui" },
    section: SECTION_ACTIVITES,
    order: 70,
  },
  {
    id: "descriptionActivite2",
    pdfFieldName: "Je décris mon activité 1",
    type: "text",
    required: false,
    label: { fr: "Je décris mon activité (suite)" },
    visibleIf: { fieldId: "autreActiviteAccessoire", op: "equals", value: "oui" },
    section: SECTION_ACTIVITES,
    order: 71,
  },
  {
    id: "descriptionActivite3",
    pdfFieldName: "Je décris mon activité 2",
    type: "text",
    required: false,
    label: { fr: "Je décris mon activité (fin)" },
    visibleIf: { fieldId: "autreActiviteAccessoire", op: "equals", value: "oui" },
    section: SECTION_ACTIVITES,
    order: 72,
  },
```

- [ ] **Step 4 : Rendre à Q2 son numéro d'entreprise et corriger les libellés d'adresse**

Dans le bloc Q2, remplacer le commentaire « A VALIDER Oraliks : le numéro
d'entreprise… » et le champ `independantAdresseRue` par :

```ts
  {
    // Le n° d'entreprise de l'indépendant aidé n'avait aucun champ : la case
    // (p1, y=342, sous son nom) est le premier widget de `TVA`, capté par Q16.
    // `TVA` couvrant les DEUX cases avec une seule valeur, écriture
    // positionnelle ici aussi — cf. le commentaire de `numeroEntreprise`.
    id: "independantNumeroEntreprise",
    pdfFieldName: "",
    drawAt: { page: 0, x: 115, y: 338, size: 9, maxWidth: 134 },
    type: "bce",
    required: false,
    label: { fr: "Numéro d'entreprise de l'indépendant que tu aides" },
    help: {
      fr: "Numéro à la Banque-Carrefour des Entreprises, au format 0123.456.789.",
    },
    visibleIf: { fieldId: "aideIndependant", op: "equals", value: "oui" },
    section: SECTION_AIDE_INDEPENDANT,
    order: 2,
  },
  {
    // `Adresse de lactivité indépendante` est posé sur la ligne « rue numéro »
    // (widget y=308, ligne y=315) : c'est une case de saisie, pas un libellé —
    // d'où le renommage de l'identifiant.
    id: "independantAdresseRueNumero",
    legacyIds: ["adresseActiviteIndependanteLabel"],
    pdfFieldName: "Adresse de lactivité indépendante",
    type: "text",
    required: false,
    label: { fr: "Adresse de l'activité indépendante — rue et numéro" },
    help: { fr: "Rue et numéro où l'indépendant exerce son activité." },
    visibleIf: { fieldId: "aideIndependant", op: "equals", value: "oui" },
    section: SECTION_ADRESSE,
    order: 3,
  },
  {
    // `rue_2` est la ligne SUIVANTE (y=284), celle du code postal et de la
    // commune — pas une suite de la rue.
    id: "independantAdresseCodePostalCommune",
    legacyIds: ["independantAdresseRue"],
    pdfFieldName: "rue_2",
    type: "text",
    required: false,
    label: { fr: "Code postal et commune de l'activité indépendante" },
    visibleIf: { fieldId: "aideIndependant", op: "equals", value: "oui" },
    section: SECTION_ADRESSE,
    order: 4,
  },
```

Décaler de +1 les `order` des cinq champs `natureActiviteIndependant1..5` qui
suivent (ils passent de 4-8 à 5-9), pour que la numérotation reste strictement
croissante.

- [ ] **Step 5 : Lancer le test de géométrie**

Run: `pnpm vitest run lib/pdf-forms/__tests__/widget-geometry.test.ts`

Expected: **les 8 tests PASS**, `c1a` compris (sa liste d'écarts est vide).

Si `c1a` échoue encore, la liste affichée nomme les paires restantes : les
traiter avec le tableau de coordonnées en tête de plan avant de continuer.

- [ ] **Step 6 : Lancer la suite complète**

Run: `pnpm vitest run lib/pdf-forms`

Expected: PASS. Le test `seeds-vs-pdf` doit rester vert — tous les nouveaux
`pdfFieldName` existent bien dans le PDF.

- [ ] **Step 7 : Commit**

```bash
git add lib/pdf-forms/seed/c1a-fields.ts lib/pdf-forms/__tests__/widget-geometry.test.ts
git commit -m "fix(c1a): realigne les cases des blocs adresse, employeur et n° d'entreprise

Le nom de l'employeur s'imprimait sur la ligne de son adresse, son adresse dans
le code postal de Q15, ce code postal dans la description d'activite de Q16, et
le n° d'entreprise de Q16 dans la case de Q2 en page 1. Cause : dans ce PDF un
widget porte le nom du texte imprime AU-DESSUS de lui, et le schema avait ete
ecrit en associant les noms aux libelles par ressemblance.

Realignement par coordonnees. Q2 recupere sa case n° d'entreprise (TVA), qui
etait captee par Q16 ; Q16 passe en ecriture positionnelle, ses cases-chiffres
n'ayant aucun widget. Q16 a bien TROIS lignes de description, pas deux.

Le test de geometrie de la tache precedente passe au vert pour c1a."
```

---

### Task 3 : Écrire les revenus de Q19 aux coordonnées

**Pourquoi.** Le champ AcroForm `voir 19` porte **quatre** widgets : trois cases
de revenu de Q19 et une ligne de la grille Q18. Quatre widgets pour un seul nom
de champ signifient une seule valeur partagée : impossible d'y écrire trois
montants différents. Le schéma actuel a donc masqué ce champ comme un artefact
et laissé les trois questions de revenu écrire dans le vide — le citoyen saisit
ce qu'il gagne, rien ne s'imprime. C'est le chiffre sur lequel l'ONEM décide de
la cumulabilité de l'activité avec les allocations.

**Files:**
- Modify: `lib/pdf-forms/seed/c1a-fields.ts` (revenus Q19 : ~lignes 809-845 · `voir19Artefact` : ~ligne 1046)
- Modify: `lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

**Interfaces:**
- Consumes: `PdfFormField.drawAt` (`lib/pdf-forms/types.ts:314`), appliqué par `lib/pdf-forms/filler.ts:996`.
- Produces: rien.

- [ ] **Step 1 : Écrire le test qui échoue**

Ajouter à `lib/pdf-forms/seed/__tests__/c1a-fields.test.ts` :

```ts
describe("C1A — revenus imprimés (Q11, Q19)", () => {
  const fields = applyC1AImprovements([]);
  const parCle = new Map(fields.map((f) => [f.id, f]));

  it("les trois revenus de Q19 sont écrits aux coordonnées, pas dans un widget partagé", () => {
    for (const id of [
      "revenuNetSalarieParMois",
      "revenuNetSalarieParHeure",
      "revenuNetIndependantParAn",
    ]) {
      const f = parCle.get(id);
      expect(f, `${id} doit exister`).toBeDefined();
      expect(f?.drawAt, `${id} doit porter un drawAt`).toBeDefined();
      expect(f?.drawAt?.page, `${id} est en page 2`).toBe(1);
      expect(f?.pdfFieldName, `${id} ne doit pointer vers aucun widget`).toBe("");
    }
  });

  it("aucun champ ne revendique le widget partagé « voir 19 »", () => {
    const revendications = fields.filter((f) => f.pdfFieldName?.includes("voir 19"));
    expect(revendications.map((f) => f.id)).toEqual([]);
  });
});
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

Run: `pnpm vitest run lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

Expected: FAIL — `revenuNetSalarieParMois doit porter un drawAt` (les trois
champs sont virtuels sans `drawAt`), et `voir19Artefact` revendique `voir 19`.

- [ ] **Step 3 : Poser les `drawAt` sur les trois revenus**

Dans `lib/pdf-forms/seed/c1a-fields.ts`, remplacer les trois champs de revenu par :

```ts
  {
    // Les trois cases de revenu de Q19 appartiennent au champ AcroForm
    // « voir 19 », qui porte QUATRE widgets partageant une seule valeur — donc
    // inutilisable pour trois montants distincts. Écriture positionnelle.
    // Coordonnées mesurées : ligne « par mois : … EUR / par heure : … EUR »
    // imprimée à y=567, ligne « par an : … » à y=497.
    id: "revenuNetSalarieParMois",
    pdfFieldName: "",
    drawAt: { page: 1, x: 360, y: 563, size: 9, maxWidth: 62 },
    type: "number",
    required: false,
    label: { fr: "Revenu net comme salarié — par mois (EUR)" },
    help: {
      fr: "Montant brut diminué des cotisations de sécurité sociale et du précompte professionnel retenus à la source par l'employeur (rémunération mensuelle normale, mais aussi pécule de vacances, 13ᵉ mois et avantages en nature éventuels). Indique jusqu'à 2 chiffres après la virgule.",
    },
    visibleIf: { fieldId: "activiteCommeSalarie", op: "equals", value: "oui" },
    section: SECTION_REVENUS,
    order: 80,
  },
  {
    id: "revenuNetSalarieParHeure",
    pdfFieldName: "",
    drawAt: { page: 1, x: 487, y: 563, size: 9, maxWidth: 46 },
    type: "number",
    required: false,
    label: { fr: "Revenu net comme salarié — par heure (EUR)" },
    help: { fr: "Indique jusqu'à 4 chiffres après la virgule." },
    visibleIf: { fieldId: "activiteCommeSalarie", op: "equals", value: "oui" },
    section: SECTION_REVENUS,
    order: 81,
  },
  {
    id: "revenuNetIndependantParAn",
    pdfFieldName: "",
    drawAt: { page: 1, x: 350, y: 493, size: 9, maxWidth: 185 },
    type: "number",
    required: false,
    label: { fr: "Revenu net comme indépendant — par an (EUR)" },
    help: {
      fr: "Revenu imposable indiqué sur l'avertissement-extrait de rôle et la note de calcul (recettes diminuées des charges, dépenses et pertes professionnelles). Joins une copie de la plus récente note de calcul de l'administration des contributions directes.",
    },
    visibleIf: { fieldId: "autreActiviteAccessoire", op: "equals", value: "oui" },
    section: SECTION_REVENUS,
    order: 82,
  },
```

- [ ] **Step 4 : Supprimer le faux artefact `voir19Artefact`**

Supprimer entièrement le champ `voir19Artefact` et le commentaire « A VALIDER
Oraliks : "voir 19" est un widget texte isolé… » qui le précède, et ajouter son
identifiant à la liste des identifiants retirés, en bas du fichier :

```ts
const LEGACY_C1A_FIELD_IDS = ["codePostalEtCommune", "code_postal_et_commune", "voir19Artefact"];
```

Le champ `voir 19` reste un widget non revendiqué du PDF, ce qui est correct : on
n'écrit plus dedans, on écrit par-dessus, aux coordonnées.

- [ ] **Step 5 : Lancer les tests**

Run: `pnpm vitest run lib/pdf-forms`

Expected: PASS, y compris le test de géométrie (les champs `drawAt` sans
`pdfFieldName` sont ignorés par l'ancre) et `seeds-vs-pdf`.

- [ ] **Step 6 : Commit**

```bash
git add lib/pdf-forms/seed/c1a-fields.ts lib/pdf-forms/seed/__tests__/c1a-fields.test.ts
git commit -m "fix(c1a): imprime enfin les revenus de Q19

Les trois cases << par mois / par heure / par an >> appartiennent au champ
AcroForm << voir 19 >>, qui porte quatre widgets partageant une seule valeur :
impossible d'y ecrire trois montants distincts. Le schema les avait donc laisses
virtuels et masquait le champ comme un artefact. Resultat : le citoyen saisissait
son revenu et rien ne s'imprimait, alors que c'est le chiffre sur lequel l'ONEM
decide de la cumulabilite.

Ecriture positionnelle (drawAt) aux coordonnees des trois lignes imprimees. Les
aides sont recopiees mot pour mot du formulaire officiel."
```

---

### Task 4 : Écrire Q10 et Q11 aux coordonnées

**Pourquoi.** La zone Q10/Q11 (page 2, colonne de gauche, y 690→800) ne contient
**aucun** widget AcroForm — vérifié. Le mandat déclaré et son revenu annuel sont
aujourd'hui saisis dans des champs virtuels et ne s'impriment jamais.

**Files:**
- Modify: `lib/pdf-forms/seed/c1a-fields.ts` (Q10-Q11 : ~lignes 576-615)
- Modify: `lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

**Interfaces:**
- Consumes: `PdfFormField.drawAt`.
- Produces: `revenuAnnuelMandat` reste un champ unique — voir la note ci-dessous.

**Question résolue depuis la spec.** Le §9 de la spec laissait ouvert si Q11
attendait un ou deux montants. La mesure tranche : le champ `Montant` porte deux
widgets dans cette zone, à (67, 707) et (67, 695) — **deux montants distincts**,
un par ligne imprimée. Ils partagent leur valeur avec le montant de Q6 (p1), donc
les trois passent en écriture positionnelle.

- [ ] **Step 1 : Écrire le test qui échoue**

Ajouter à `lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`, dans le `describe`
créé à la Task 3 :

```ts
  it("le mandat et son revenu (Q10, Q11) sont écrits aux coordonnées", () => {
    for (const id of ["mandatDescription", "revenuAnnuelMandat", "revenuAnnuelMandat2"]) {
      const f = parCle.get(id);
      expect(f, `${id} doit exister`).toBeDefined();
      expect(f?.drawAt, `${id} doit porter un drawAt`).toBeDefined();
      expect(f?.drawAt?.page, `${id} est en page 2`).toBe(1);
    }
  });

  it("aucun champ ne revendique le widget partagé « Montant »", () => {
    expect(fields.filter((f) => f.pdfFieldName === "Montant").map((f) => f.id)).toEqual([]);
  });
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

Run: `pnpm vitest run lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

Expected: FAIL — `mandatDescription doit porter un drawAt`.

- [ ] **Step 3 : Poser les `drawAt`**

Remplacer les champs `mandatDescription` et `revenuAnnuelMandat` par :

```ts
  {
    // Q10 et Q11 n'ont AUCUN widget AcroForm (vérifié : rien en page 2, colonne
    // de gauche, entre y=690 et y=800). Écriture positionnelle sur les lignes
    // imprimées : Q10 occupe trois lignes entre y=779 et y=743, Q11 deux lignes
    // « EUR … EUR » à y=714 et y=702.
    id: "mandatDescription",
    pdfFieldName: "",
    drawAt: { page: 1, x: 50, y: 766, size: 9, maxWidth: 236 },
    type: "text",
    required: false,
    label: { fr: "10. Quel mandat ou quelle fonction ?" },
    help: {
      fr: "Si tu exerces plus d'un mandat ou as plus d'une fonction, mentionne-les tous.",
    },
    visibleIf: { fieldId: "mandatPolitiqueOuJuge", op: "equals", value: "oui" },
    section: SECTION_ACTIVITES,
    order: 55,
  },
  {
    // Q11 imprime deux lignes de montant, matérialisées par les 2ᵉ et 3ᵉ
    // widgets du champ `Montant` — dont le 1ᵉʳ est le montant de Q6, en page 1.
    // Une seule valeur pour les trois : écriture positionnelle obligatoire.
    id: "revenuAnnuelMandat",
    pdfFieldName: "",
    drawAt: { page: 1, x: 69, y: 703, size: 9, maxWidth: 62 },
    type: "number",
    required: false,
    label: { fr: "11. Revenu annuel net imposable de ce mandat (EUR)" },
    help: {
      fr: "Joins une copie de la plus récente note de calcul de l'administration des contributions directes.",
    },
    visibleIf: { fieldId: "mandatPolitiqueOuJuge", op: "equals", value: "oui" },
    section: SECTION_REVENUS,
    order: 56,
  },
  {
    // A VALIDER Oraliks : la seconde ligne de Q11. Le formulaire prévoit deux
    // montants sans préciser à l'impression ce qui les distingue — un second
    // mandat, ou une seconde composante du même revenu.
    id: "revenuAnnuelMandat2",
    pdfFieldName: "",
    drawAt: { page: 1, x: 69, y: 691, size: 9, maxWidth: 62 },
    type: "number",
    required: false,
    label: { fr: "Second montant, si tu exerces plus d'un mandat (EUR)" },
    visibleIf: { fieldId: "mandatPolitiqueOuJuge", op: "equals", value: "oui" },
    section: SECTION_REVENUS,
    order: 57,
  },
```

- [ ] **Step 4 : Lancer les tests**

Run: `pnpm vitest run lib/pdf-forms`

Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
git add lib/pdf-forms/seed/c1a-fields.ts lib/pdf-forms/seed/__tests__/c1a-fields.test.ts
git commit -m "fix(c1a): imprime le mandat et son revenu (Q10, Q11)

La zone Q10/Q11 ne contient aucun widget AcroForm : les deux questions etaient
saisies dans des champs virtuels et ne s'imprimaient jamais. Ecriture
positionnelle sur les lignes imprimees.

Q11 imprime DEUX lignes EUR ; un seul montant est ecrit, sur la premiere. A
trancher avec Oraliks sur l'exemplaire papier avant de scinder."
```

---

### Task 5 : Scinder le montant de Q6 et imprimer le nombre d'annexes

**Pourquoi.** Deux valeurs que le citoyen saisit et qui ne s'impriment pas.

Q6 attend un montant « par mois » ou « par an » : un seul champ `montantAide`
existe, et le citoyen ne peut pas indiquer lequel des deux il déclare.

Q24 imprime « Je joins …… annexe(s) » et cette case **a bien un widget** —
`Liste déroulante44`, à (350, 171), juste au-dessus de la ligne. Le schéma l'a
classée « champ non identifié » et masquée, tandis que `nombreAnnexesJointes`
reste virtuel : le nombre d'annexes ne s'imprime jamais. Oraliks a tranché que
les annexes sont facultatives **mais que la case doit exister**.

**Files:**
- Modify: `lib/pdf-forms/seed/c1a-fields.ts` (~ligne 506)
- Modify: `lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

**Interfaces:**
- Consumes: rien de nouveau.
- Produces: `montantAidePeriodicite` (radio `mois` | `an`).

**Mesures.** Le widget `Montant` couvre trois emplacements (Q6 en p1, les deux
montants de Q11 en p2) pour une seule valeur : Q6 passe donc en `drawAt` comme
Q11. Sur la page 1, la question a **une seule ligne pointillée** (y=315, de
x=318 à x=559) sous-titrée « par mois EUR … par an EUR » (« par mois » à x=317,
« par an » à x=503). Les deux montants s'écrivent donc sur cette même ligne, à
gauche et à droite.

Le calage horizontal exact des deux montants est le point le plus incertain de
ce lot : à confirmer sur le PDF généré (Task 6, Step 4).

- [ ] **Step 1 : Écrire le test qui échoue**

```ts
  it("Q6 distingue le montant mensuel du montant annuel", () => {
    const periodicite = parCle.get("montantAidePeriodicite");
    expect(periodicite?.type).toBe("radio");
    expect(periodicite?.options?.map((o) => o.value)).toEqual(["mois", "an"]);
  });

  it("le nombre d'annexes s'imprime et reste facultatif", () => {
    const annexes = parCle.get("nombreAnnexesJointes");
    expect(annexes?.pdfFieldName).toBe("Liste déroulante44");
    expect(annexes?.required, "Oraliks : facultatif").not.toBe(true);
    expect(annexes?.hidden, "mais la case doit exister à l'écran").not.toBe(true);
  });

  it("plus aucun champ « non identifié » ne squatte la case des annexes", () => {
    expect(fields.filter((f) => f.id === "listeDeroulante44")).toEqual([]);
  });
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

Run: `pnpm vitest run lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

Expected: FAIL — `expected undefined to be 'radio'`.

- [ ] **Step 3 : Scinder le champ**

Remplacer le champ `montantAide` par :

```ts
  {
    // Q6 attend un montant « par mois » OU « par an » sur une même ligne
    // pointillée. Le widget `Montant` couvre cette ligne ET les deux montants
    // de Q11 en page 2, avec une seule valeur partagée — donc inexploitable.
    id: "montantAidePeriodicite",
    pdfFieldName: "",
    type: "radio",
    required: false,
    label: { fr: "6. Ce montant est :" },
    options: [
      { value: "mois", label: { fr: "Par mois" } },
      { value: "an", label: { fr: "Par an" } },
    ],
    visibleIf: { fieldId: "aideraPendantChomage", op: "equals", value: "oui" },
    section: SECTION_REVENUS,
    order: 40,
  },
  {
    id: "montantAide",
    pdfFieldName: "",
    // Moitié gauche de la ligne pointillée (y=315), au-dessus de « par mois ».
    drawAt: { page: 0, x: 322, y: 311, size: 9, maxWidth: 110 },
    type: "number",
    required: false,
    label: { fr: "Combien gagnes-tu pour ton aide ? (EUR)" },
    help: {
      fr: "Ou à combien s'élève la valeur de ton aide. Joins une copie de la plus récente note de calcul de l'administration des contributions directes.",
    },
    visibleIf: { fieldId: "montantAidePeriodicite", op: "equals", value: "mois" },
    section: SECTION_REVENUS,
    order: 41,
  },
  {
    id: "montantAideAnnuel",
    pdfFieldName: "",
    // Moitié droite de la même ligne, avant la légende « par an » (x=503).
    drawAt: { page: 0, x: 440, y: 311, size: 9, maxWidth: 58 },
    type: "number",
    required: false,
    label: { fr: "Combien gagnes-tu pour ton aide ? (EUR)" },
    help: {
      fr: "Ou à combien s'élève la valeur de ton aide. Joins une copie de la plus récente note de calcul de l'administration des contributions directes.",
    },
    visibleIf: { fieldId: "montantAidePeriodicite", op: "equals", value: "an" },
    section: SECTION_REVENUS,
    order: 42,
  },
```

- [ ] **Step 4 : Rendre le nombre d'annexes imprimable**

Remplacer le champ `nombreAnnexesJointes` par :

```ts
  {
    // La case « Je joins …… annexe(s) » de Q24 a bien un widget :
    // `Liste déroulante44`, à (350, 171), juste au-dessus de la ligne imprimée
    // (y=179). Le schéma l'avait classée « champ non identifié » et masquée,
    // et ce champ-ci restait virtuel — le nombre ne s'imprimait jamais.
    //
    // Facultatif (décision Oraliks), mais la case doit exister à l'écran.
    id: "nombreAnnexesJointes",
    pdfFieldName: "Liste déroulante44",
    type: "number",
    required: false,
    label: { fr: "Nombre d'annexes jointes" },
    help: {
      fr: "Par exemple la copie de la plus récente note de calcul de l'administration des contributions directes, demandée aux questions 6, 11 et 19.",
    },
    section: SECTION_ANNEXES,
    order: 200,
  },
```

Puis **supprimer** le champ `listeDeroulante44` et le commentaire « A VALIDER
Oraliks : "Liste déroulante44" … » qui le précède, et ajouter son identifiant
aux identifiants retirés :

```ts
const LEGACY_C1A_FIELD_IDS = [
  "codePostalEtCommune",
  "code_postal_et_commune",
  "voir19Artefact",
  "listeDeroulante44",
];
```

- [ ] **Step 5 : Lancer les tests**

Run: `pnpm vitest run lib/pdf-forms`

Expected: PASS.

- [ ] **Step 6 : Commit**

```bash
git add lib/pdf-forms/seed/c1a-fields.ts lib/pdf-forms/seed/__tests__/c1a-fields.test.ts
git commit -m "fix(c1a): imprime le montant de Q6 et le nombre d'annexes de Q24

Q6 attend un montant par mois OU par an sur une meme ligne : un seul champ
existait, sans moyen d'indiquer lequel. Une question de periodicite ouvre
desormais le bon montant. Le widget Montant couvrant aussi les deux montants de
Q11 en page 2 avec une valeur partagee, les trois passent en ecriture
positionnelle.

Q24 : la case << Je joins ... annexe(s) >> a bien un widget, classe a tort
<< champ non identifie >> et masque, pendant que le champ de saisie restait
virtuel. Le nombre ne s'imprimait jamais."
```

---

### Task 6 : Re-semer et contrôler le PDF avec Oraliks

**Files:**
- Aucun fichier modifié — tâche de vérification.

**Interfaces:**
- Consumes: les tâches 2 à 5.
- Produces: le feu vert pour le lot 2.

- [ ] **Step 1 : Vérifier la suite complète et le build**

```bash
pnpm test
```

Expected: PASS, aucune régression sur les 1949 tests.

```bash
pnpm build
```

Expected: build réussi (typecheck compris).

- [ ] **Step 2 : Re-semer le schéma en base**

Le runner lit la DB, pas le code. Sans cette étape, l'écran ne changera pas.

```bash
pnpm tsx scripts/apply-c1-improvements.ts
```

Expected: aperçu listant `c1a` avec un nombre de champs avant/après.

```bash
pnpm tsx scripts/apply-c1-improvements.ts --yes
```

Expected: `c1a` → `updated`.

- [ ] **Step 3 : Générer un C1A de test**

Lancer le serveur de développement, ouvrir `/document/c1a`, et remplir un cas qui
traverse **les trois branches** :
- Q1 = oui, Q3 = oui → grille horaire, description, montant, Q7 = oui + date
- Q9 = oui → mandat + revenu annuel
- Q12 = oui, Q13 = oui → employeur, adresse, Q16 mandataire + n° d'entreprise, Q17 = oui → grille, revenus, Q20 = oui + date

Télécharger le PDF produit.

- [ ] **Step 4 : Relecture case par case avec Oraliks — BLOQUANT**

Comparer le PDF généré au formulaire officiel, case par case. Vérifier
nommément :

| À vérifier | Attendu |
|---|---|
| Nom de l'employeur | ligne « nom » de Q14, pas la ligne « adresse » |
| Adresse de l'employeur | ligne « adresse » de Q14 |
| Adresse de l'activité (Q15) | rue+numéro sur la 1ʳᵉ ligne, code postal+commune sur la 2ᵉ |
| N° d'entreprise de l'indépendant aidé | cases de la page 1, sous son nom |
| N° d'entreprise de l'activité (Q16) | cases de la page 2, après « oui » |
| Description de l'activité | trois lignes de Q16, aucune écrasée |
| Revenus Q19 | trois montants à leur place, aucun débordement |
| Mandat et revenu Q10/Q11 | sur les lignes imprimées, dans la marge de la colonne |

**Ne pas passer au lot 2 tant qu'Oraliks n'a pas validé ce PDF.** Aucun test ne
peut certifier qu'une déclaration officielle est correcte ; il peut seulement
dire qu'une case est là où le texte l'annonce.

- [ ] **Step 5 : Consigner le résultat**

Si des écarts subsistent, les corriger et refaire les steps 1 à 4. Si tout est
conforme, noter la validation dans la spec :

```bash
git add docs/superpowers/specs/2026-07-28-c1a-refonte-design.md
git commit -m "docs(c1a): lot 1 valide sur PDF genere, relu avec Oraliks"
```

---

# LOT 2 — l'arbre et l'écran

### Task 7 : Extraire les moules partagés

**Pourquoi et jusqu'où.** Le partage réel est plus mince que l'intuition ne le
suggère : `dejaDeclare` et `dateAPartirDu` n'ont aucun équivalent chez les sept
compagnons, et `ouiNon` fige trois réglages (`defaultValue: "non"`,
`stepPriority: "optional"`, `required: true`) qu'**aucun** compagnon ne partage —
huit de leurs questions sont volontairement facultatives. L'imposer tel quel
changerait ce que voit le citoyen **sans qu'aucun test ne le détecte**.

On partage donc : les constantes de section, `YN` (trilingue côté C1, français
seul dans les six copies), et `ouiNon` **paramétré**, ses valeurs actuelles
restant les valeurs par défaut.

**Files:**
- Create: `lib/pdf-forms/seed/_shared/moules.ts`
- Modify: `lib/pdf-forms/seed/c1/helpers.ts`

**Interfaces:**
- Produces: `SECTION_*` (19 constantes), `YN`, `YN_DECLARE`, `ouiNon(opts)` avec `required?: boolean` `defaultValue?: string` `stepPriority?: PdfFormField["stepPriority"]`.
- Consumes: `PdfFormField` (`lib/pdf-forms/types.ts`).

- [ ] **Step 1 : Créer le module partagé**

Créer `lib/pdf-forms/seed/_shared/moules.ts`. Y **déplacer sans les modifier**
les constantes de section, `YN`, `YN_DECLARE`, `dateAPartirDu`, `annexeJointe` et
`dejaDeclare` depuis `lib/pdf-forms/seed/c1/helpers.ts`, puis y ajouter les sept
sections des compagnons et paramétrer `ouiNon` :

```ts
// Moules de champs partagés par tous les formulaires ONEM.
//
// Ils vivaient dans `seed/c1/helpers.ts`, où le C1 en était propriétaire. Les
// sept compagnons en réimplémentaient chacun une copie locale — avec des
// divergences silencieuses (leur `YN` est monolingue là où celui du C1 est
// trilingue). Un seul exemplaire ici, et le C1 en devient un consommateur.

import type { PdfFormField } from "../../types";

// Sections du C1
export const SECTION_IDENTITE = "identite";
export const SECTION_DEMANDE = "demande";
export const SECTION_SITUATION_FAMILIALE = "situation-familiale";
export const SECTION_ACTIVITES = "mes-activites";
export const SECTION_REVENUS = "mes-revenus";
export const SECTION_PAIEMENT = "mode-paiement";
export const SECTION_COTISATION = "cotisation-syndicale";
export const SECTION_NON_EEE = "non-eee";
export const SECTION_DIVERS = "divers";
export const SECTION_AFFIRMATIONS = "affirmations";
export const SECTION_ANNEXES = "annexes";
export const SECTION_SIGNATURE = "signature";

// Sections propres aux compagnons
export const SECTION_ADRESSE = "adresse";
export const SECTION_EMPLOYEUR = "employeur";
export const SECTION_AIDE_INDEPENDANT = "aide-independant";
export const SECTION_ACTIVITES_ANTERIEURES = "activites-anterieures";
export const SECTION_MANDAT_CULTUREL = "mandat-culturel";
export const SECTION_PARTENAIRE = "partenaire";
export const SECTION_GRILLE_DIFFERENCES = "grille-differences";

/// Options communes oui/non. Premier élément = mappé à la case "oui_N",
/// second élément = mappé à la case "non_N".
export const YN = [
  { value: "oui", label: { fr: "Oui", nl: "Ja", de: "Ja" } },
  { value: "non", label: { fr: "Non", nl: "Nee", de: "Nein" } },
];

/// Options du follow-up "déjà déclaré à l'organisme de paiement ?".
export const YN_DECLARE = [
  { value: "oui", label: { fr: "Oui, déjà déclaré à l'organisme de paiement" } },
  { value: "non", label: { fr: "Non, à compléter maintenant" } },
];

/// Question officielle oui/non — une paire de cases `oui_N`/`non_N` sur le PDF.
///
/// Les trois réglages du C1 (`required`, `defaultValue`, `stepPriority`) sont
/// PARAMÉTRABLES, ses valeurs actuelles restant les valeurs par défaut : les
/// compagnons ont des questions volontairement facultatives, et hériter des
/// réglages du C1 pré-cocherait « non » chez eux et déplacerait leurs questions
/// dans l'accordéon « Autres informations ».
export function ouiNon(opts: {
  id: string;
  /// Paire de widgets, dans l'ordre des options : "oui_N|non_N".
  pdfFieldName: string;
  label: string;
  help?: string;
  section: string;
  order: number;
  required?: boolean;
  defaultValue?: string | null;
  stepPriority?: PdfFormField["stepPriority"] | null;
}): PdfFormField {
  return {
    id: opts.id,
    pdfFieldName: opts.pdfFieldName,
    type: "radio",
    required: opts.required ?? true,
    label: { fr: opts.label },
    ...(opts.help ? { help: { fr: opts.help } } : {}),
    options: YN,
    ...(opts.defaultValue === null ? {} : { defaultValue: opts.defaultValue ?? "non" }),
    section: opts.section,
    order: opts.order,
    ...(opts.stepPriority === null ? {} : { stepPriority: opts.stepPriority ?? "optional" }),
  };
}
```

Reprendre ensuite `dateAPartirDu`, `annexeJointe` et `dejaDeclare` **à
l'identique** depuis `c1/helpers.ts` (corps inchangé, seul l'import de
`PdfFormField` change de chemin).

- [ ] **Step 2 : Transformer `c1/helpers.ts` en façade**

Remplacer intégralement le contenu de `lib/pdf-forms/seed/c1/helpers.ts` par :

```ts
// Façade historique : le contenu vit désormais dans `../_shared/moules.ts`,
// partagé avec les sept formulaires compagnons. Six modules `c1/*` importent
// depuis ici, et rien ne justifiait de leur faire changer d'adresse.

export {
  SECTION_IDENTITE,
  SECTION_DEMANDE,
  SECTION_SITUATION_FAMILIALE,
  SECTION_ACTIVITES,
  SECTION_REVENUS,
  SECTION_PAIEMENT,
  SECTION_COTISATION,
  SECTION_NON_EEE,
  SECTION_DIVERS,
  SECTION_AFFIRMATIONS,
  SECTION_ANNEXES,
  SECTION_SIGNATURE,
  YN,
  YN_DECLARE,
  dateAPartirDu,
  annexeJointe,
  ouiNon,
  dejaDeclare,
} from "../_shared/moules";
```

- [ ] **Step 3 : Vérifier que le C1 n'a pas bougé d'un octet**

Run: `pnpm vitest run lib/pdf-forms/seed/__tests__/c1-fields-improvements.test.ts`

Expected: PASS. Ce test épingle les valeurs du C1 : les 15 questions
activité/revenu en `required: true` + `defaultValue: "non"`, les 7 suivis
`*DejaDeclare` sans `defaultValue`, et les sections `mes-activites |
mes-revenus | cotisation-syndicale | non-eee | divers | annexes` en
`stepPriority: "optional"`. Toute dérive du paramétrage le fait rougir.

- [ ] **Step 4 : Lancer la suite complète**

Run: `pnpm test`

Expected: PASS, aucune régression.

- [ ] **Step 5 : Commit**

```bash
git add lib/pdf-forms/seed/_shared/moules.ts lib/pdf-forms/seed/c1/helpers.ts
git commit -m "refactor(pdf): sort les moules de champs du C1 vers un module partage

Les sept compagnons reimplementaient chacun leur copie des constantes de section
et du tableau Oui/Non, avec des divergences silencieuses (copies monolingues la
ou le C1 est trilingue). Un seul exemplaire desormais, le C1 en devient un
consommateur.

ouiNon devient parametrable sur required / defaultValue / stepPriority, les
valeurs du C1 restant les valeurs par defaut : les compagnons ont des questions
volontairement facultatives, et heriter des reglages du C1 pre-cocherait << non >>
chez eux. Deplacement pur, aucun champ modifie."
```

---

### Task 8 : Le compilateur d'arbre

**Pourquoi.** Écrire les conditions d'affichage à la main oblige à se souvenir de
toutes les dépendances transitives : pour que Q8 s'affiche correctement il faut
Q1 = oui **et** Q3 = oui **et** Q7 = oui. C'est ainsi que le schéma actuel a
accroché Q4-Q8 à Q1 en oubliant Q3. On décrit donc l'arbre **tel qu'il est
imprimé** et une fonction pure en dérive les conditions.

**Files:**
- Create: `lib/pdf-forms/routing.ts`
- Create: `lib/pdf-forms/__tests__/routing.test.ts`

**Interfaces:**
- Produces:
  - `type Renvoi = { next: string } | { on: Record<string, string> }`
  - `type TableRoutage = Record<string, Renvoi>` — clé = identifiant de question
  - `function compilerRoutage(table: TableRoutage, depart: string): Record<string, VisibleIf | undefined>`
- Consumes: `VisibleIf` (`lib/pdf-forms/types.ts:139`, avec `and?: VisibleIf[]`).

- [ ] **Step 1 : Écrire les tests qui échouent**

Créer `lib/pdf-forms/__tests__/routing.test.ts` :

```ts
import { describe, it, expect } from "vitest";
import { compilerRoutage, type TableRoutage } from "../routing";

describe("compilerRoutage", () => {
  it("une question sur tous les chemins n'a aucune condition", () => {
    const table: TableRoutage = {
      q1: { on: { oui: "q2", non: "q3" } },
      q2: { next: "q3" },
      q3: { next: "fin" },
    };
    const conditions = compilerRoutage(table, "q1");
    expect(conditions.q1).toBeUndefined();
    expect(conditions.q3).toBeUndefined();
  });

  it("une question atteignable par une seule branche porte sa condition", () => {
    const table: TableRoutage = {
      q1: { on: { oui: "q2", non: "q3" } },
      q2: { next: "q3" },
      q3: { next: "fin" },
    };
    const conditions = compilerRoutage(table, "q1");
    expect(conditions.q2).toEqual({ fieldId: "q1", op: "equals", value: "oui" });
  });

  it("cumule les conditions transitives sur une branche profonde", () => {
    const table: TableRoutage = {
      q1: { on: { oui: "q2", non: "q9" } },
      q2: { next: "q3" },
      q3: { on: { oui: "q4", non: "q9" } },
      q4: { next: "q7" },
      q7: { on: { oui: "q8", non: "q9" } },
      q8: { next: "q9" },
      q9: { next: "fin" },
    };
    const conditions = compilerRoutage(table, "q1");
    expect(conditions.q4).toEqual({
      fieldId: "q3",
      op: "equals",
      value: "oui",
      and: [{ fieldId: "q1", op: "equals", value: "oui" }],
    });
    expect(conditions.q8).toEqual({
      fieldId: "q7",
      op: "equals",
      value: "oui",
      and: [
        { fieldId: "q3", op: "equals", value: "oui" },
        { fieldId: "q1", op: "equals", value: "oui" },
      ],
    });
    expect(conditions.q9, "q9 est sur tous les chemins").toBeUndefined();
  });

  it("refuse un renvoi vers une question inconnue", () => {
    const table: TableRoutage = { q1: { next: "q404" } };
    expect(() => compilerRoutage(table, "q1")).toThrow(/q404/);
  });

  it("refuse un cycle", () => {
    const table: TableRoutage = { q1: { next: "q2" }, q2: { next: "q1" } };
    expect(() => compilerRoutage(table, "q1")).toThrow(/cycle/i);
  });
});
```

- [ ] **Step 2 : Lancer les tests pour vérifier qu'ils échouent**

Run: `pnpm vitest run lib/pdf-forms/__tests__/routing.test.ts`

Expected: FAIL — `Cannot find module '../routing'`.

- [ ] **Step 3 : Implémenter le compilateur**

Créer `lib/pdf-forms/routing.ts` :

```ts
// Compilateur d'arbre de renvois.
//
// Les formulaires ONEM ne sont pas des questionnaires plats : ils impriment des
// renvois (« oui → voir 2 · non → voir 9 »), et les questions sautées ne
// doivent pas être remplies. Décrire cet arbre revient à recopier ce qui est
// imprimé ; en déduire les conditions d'affichage, en revanche, demande de
// suivre toutes les dépendances transitives — Q8 exige Q7 = oui ET Q3 = oui ET
// Q1 = oui. C'est là que la main se trompe, et c'est ce que cette fonction fait
// à sa place.
//
// Le résultat n'a jamais besoin de disjonction : une question atteignable par
// plusieurs chemins l'est, dans ces formulaires, par TOUS les chemins — elle
// est donc simplement toujours visible.

import type { VisibleIf } from "./types";

/// Renvoi d'une question : soit une suite unique, soit un aiguillage par valeur.
export type Renvoi = { next: string } | { on: Record<string, string> };

/// Arbre complet, recopié du document : clé = identifiant du champ qui porte la
/// question, valeur = son renvoi imprimé.
export type TableRoutage = Record<string, Renvoi>;

/// Marqueur de fin de parcours — une cible qui n'est pas une question.
export const FIN = "fin";

type Chemin = Array<{ fieldId: string; value: string }>;

function estAiguillage(r: Renvoi): r is { on: Record<string, string> } {
  return "on" in r;
}

/// Conditions d'affichage de chaque question, dérivées de l'arbre.
/// `undefined` = toujours visible.
export function compilerRoutage(
  table: TableRoutage,
  depart: string,
): Record<string, VisibleIf | undefined> {
  // Chemins d'accès distincts menant à chaque question.
  const acces = new Map<string, Chemin[]>();

  function parcourir(question: string, chemin: Chemin, pile: string[]): void {
    if (question === FIN) return;
    if (pile.includes(question)) {
      throw new Error(`Cycle détecté dans l'arbre de routage : ${[...pile, question].join(" → ")}`);
    }
    const renvoi = table[question];
    if (!renvoi) {
      throw new Error(`Renvoi vers une question inconnue : « ${question} »`);
    }

    const dejaVus = acces.get(question) ?? [];
    dejaVus.push(chemin);
    acces.set(question, dejaVus);

    const pileSuivante = [...pile, question];
    if (estAiguillage(renvoi)) {
      for (const [valeur, cible] of Object.entries(renvoi.on)) {
        parcourir(cible, [...chemin, { fieldId: question, value: valeur }], pileSuivante);
      }
    } else {
      parcourir(renvoi.next, chemin, pileSuivante);
    }
  }

  parcourir(depart, [], []);

  const conditions: Record<string, VisibleIf | undefined> = {};
  for (const [question, chemins] of acces) {
    // Conditions communes à TOUS les chemins menant ici. Ce qui n'est pas
    // commun ne peut pas être exprimé sans disjonction — et n'apparaît pas
    // dans ces formulaires.
    const premier = chemins[0];
    const communes = premier.filter((cond) =>
      chemins.every((c) => c.some((x) => x.fieldId === cond.fieldId && x.value === cond.value)),
    );
    if (communes.length === 0) {
      conditions[question] = undefined;
      continue;
    }
    // La condition la plus proche en tête, les ancêtres dans `and` : c'est
    // l'ordre de lecture naturel quand on relit le schéma.
    const [proche, ...ancetres] = [...communes].reverse();
    conditions[question] = {
      fieldId: proche.fieldId,
      op: "equals",
      value: proche.value,
      ...(ancetres.length
        ? { and: ancetres.map((a) => ({ fieldId: a.fieldId, op: "equals" as const, value: a.value })) }
        : {}),
    };
  }
  return conditions;
}
```

- [ ] **Step 4 : Lancer les tests**

Run: `pnpm vitest run lib/pdf-forms/__tests__/routing.test.ts`

Expected: PASS, 5 tests.

- [ ] **Step 5 : Commit**

```bash
git add lib/pdf-forms/routing.ts lib/pdf-forms/__tests__/routing.test.ts
git commit -m "feat(pdf): compilateur d'arbre de renvois

Les formulaires ONEM impriment des renvois (<< oui voir 2 / non voir 9 >>) et les
questions sautees ne doivent pas etre remplies. Recopier l'arbre est trivial ;
en deduire les conditions d'affichage demande de suivre toutes les dependances
transitives (Q8 exige Q7 ET Q3 ET Q1), et c'est la que la main se trompe — le
C1A accroche aujourd'hui Q4-Q8 a Q1 en oubliant Q3.

Fonction pure : table recopiee du document en entree, conditions derivees en
sortie. Detecte les cycles et les renvois vers une question inconnue."
```

---

### Task 9 : Appliquer l'arbre du C1A

**Files:**
- Create: `lib/pdf-forms/seed/c1a-routing.ts`
- Modify: `lib/pdf-forms/seed/c1a-fields.ts`
- Modify: `lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

**Interfaces:**
- Consumes: `compilerRoutage`, `TableRoutage`, `FIN` (Task 8).
- Produces: `C1A_ROUTAGE` (`TableRoutage`), `C1A_DEPART` (`"aideIndependant"`).

- [ ] **Step 1 : Écrire la table de routage**

Créer `lib/pdf-forms/seed/c1a-routing.ts` :

```ts
// Arbre des renvois imprimés du C1A, recopié du PDF officiel.
//
// Chaque arête vient d'un « voir N » imprimé : AUCUN enchaînement implicite.
// Les deux seules questions sans renvoi sont Q23 et Q24, terminales, qui
// portent leur propre consigne « COMPLÉTEZ TOUJOURS CETTE RUBRIQUE ».
//
// Version imprimée : 01.11.2013 / 830.10.002.

import { FIN, type TableRoutage } from "../routing";

export const C1A_DEPART = "aideIndependant";

export const C1A_ROUTAGE: TableRoutage = {
  // Q1 → Q2 / Q9
  aideIndependant: { on: { oui: "independantNom", non: "mandatPolitiqueOuJuge" } },
  // Q2 → Q3
  independantNom: { next: "aideraPendantChomage" },
  // Q3 → Q4 / Q9
  aideraPendantChomage: { on: { oui: "q4lundi", non: "mandatPolitiqueOuJuge" } },
  // Q4 → Q5
  q4lundi: { next: "descriptionAide1" },
  // Q5 → Q6
  descriptionAide1: { next: "montantAidePeriodicite" },
  // Q6 → Q7
  montantAidePeriodicite: { next: "aidaitDejaIndependant" },
  // Q7 → Q8 / Q9
  aidaitDejaIndependant: { on: { oui: "dateDebutAide", non: "mandatPolitiqueOuJuge" } },
  // Q8 → Q9
  dateDebutAide: { next: "mandatPolitiqueOuJuge" },
  // Q9 → Q10 / Q12. Exemption imprimée : conseiller communal, conseiller
  // provincial, membre d'un CPAS, juge social, juge consulaire ou conseiller
  // social → répondre « non ».
  mandatPolitiqueOuJuge: { on: { oui: "mandatDescription", non: "autreActiviteAccessoire" } },
  // Q10 → Q11
  mandatDescription: { next: "revenuAnnuelMandat" },
  // Q11 → Q12
  revenuAnnuelMandat: { next: "autreActiviteAccessoire" },
  // Q12 → Q13 / Q22
  autreActiviteAccessoire: { on: { oui: "activiteCommeSalarie", non: "estChomeurTemporaire" } },
  // Q13 → Q14 / Q15
  activiteCommeSalarie: { on: { oui: "employeurNom", non: "adresseActivite" } },
  // Q14 → Q15
  employeurNom: { next: "adresseActivite" },
  // Q15 → Q16
  adresseActivite: { next: "formeActivite" },
  // Q16 → Q17. UNE seule sortie pour les deux options — pas de renvoi par
  // option, contrairement à Q1 ou Q13.
  formeActivite: { next: "exerceraPendantChomage" },
  // Q17 → Q18 / Q22
  exerceraPendantChomage: { on: { oui: "q18lundi", non: "estChomeurTemporaire" } },
  // Q18 → Q19
  q18lundi: { next: "revenuNetSalarieParMois" },
  // Q19 → Q20
  revenuNetSalarieParMois: { next: "exerceDejaActivite" },
  // Q20 → Q21 / Q22
  exerceDejaActivite: { on: { oui: "dateDebutActivite", non: "estChomeurTemporaire" } },
  // Q21 → Q22
  dateDebutActivite: { next: "estChomeurTemporaire" },
  // Q22 → Q23. Rubrique réservée aux chômeurs temporaires : la question
  // d'entrée `estChomeurTemporaire` n'existe pas sur le papier, elle
  // matérialise la consigne imprimée « À COMPLÉTER UNIQUEMENT SI… ».
  estChomeurTemporaire: { on: { oui: "joursOccupeLundi", non: "independantTitrePrincipal" } },
  joursOccupeLundi: { next: "independantTitrePrincipal" },
  // Q23 et Q24 — terminales, « COMPLÉTEZ TOUJOURS CETTE RUBRIQUE ».
  independantTitrePrincipal: { next: "affirmationSincerite" },
  affirmationSincerite: { next: FIN },
};
```

- [ ] **Step 2 : Écrire le test qui échoue**

Ajouter à `lib/pdf-forms/seed/__tests__/c1a-fields.test.ts` :

```ts
import { compilerRoutage } from "../../routing";
import { C1A_ROUTAGE, C1A_DEPART } from "../c1a-routing";

describe("C1A — arbre des renvois", () => {
  const conditions = compilerRoutage(C1A_ROUTAGE, C1A_DEPART);
  const fields = applyC1AImprovements([]);
  const parCle = new Map(fields.map((f) => [f.id, f]));

  it("Q9, Q12 et Q22 sont sur tous les chemins, donc sans condition", () => {
    expect(conditions.mandatPolitiqueOuJuge).toBeUndefined();
    expect(conditions.autreActiviteAccessoire).toBeUndefined();
    expect(conditions.estChomeurTemporaire).toBeUndefined();
  });

  it("Q4 dépend de Q3 ET de Q1 — c'est le bug historique", () => {
    expect(conditions.q4lundi).toEqual({
      fieldId: "aideraPendantChomage",
      op: "equals",
      value: "oui",
      and: [{ fieldId: "aideIndependant", op: "equals", value: "oui" }],
    });
  });

  it("le schéma porte les conditions compilées", () => {
    expect(parCle.get("q4lundi")?.visibleIf).toEqual(conditions.q4lundi);
    expect(parCle.get("descriptionAide1")?.visibleIf).toEqual(conditions.descriptionAide1);
    expect(parCle.get("mandatPolitiqueOuJuge")?.visibleIf).toBeUndefined();
  });
});
```

- [ ] **Step 3 : Lancer le test pour vérifier qu'il échoue**

Run: `pnpm vitest run lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

Expected: FAIL sur « le schéma porte les conditions compilées » — `q4lundi`
dépend aujourd'hui de `aideraPendantChomage` seul, sans l'ancêtre `Q1`.

- [ ] **Step 4 : Appliquer les conditions compilées au schéma**

Dans `lib/pdf-forms/seed/c1a-fields.ts`, à la fin de `applyC1AImprovements`,
juste avant le `return`, appliquer les conditions dérivées aux champs de tête de
question et à tous ceux qui en dépendent :

```ts
import { compilerRoutage } from "../routing";
import { C1A_ROUTAGE, C1A_DEPART } from "./c1a-routing";

/// Champs qui suivent la même condition que la question qui les porte : les
/// lignes d'une même rubrique du PDF s'affichent ou disparaissent ensemble.
const RATTACHEMENTS: Record<string, string[]> = {
  independantNom: [
    "independantNumeroEntreprise",
    "independantAdresseRueNumero",
    "independantAdresseCodePostalCommune",
    "natureActiviteIndependant1",
    "natureActiviteIndependant2",
    "natureActiviteIndependant3",
    "natureActiviteIndependant4",
    "natureActiviteIndependant5",
  ],
  descriptionAide1: [
    "descriptionAide2", "descriptionAide3", "descriptionAide4", "descriptionAide5",
    "descriptionAide6", "descriptionAide7", "descriptionAide8", "descriptionAide9",
  ],
  montantAidePeriodicite: ["montantAide", "montantAideAnnuel"],
  mandatDescription: [],
  employeurNom: ["employeurAdresse"],
  adresseActivite: ["adresseActiviteCodePostalCommune"],
  formeActivite: [
    "disposeNumeroEntreprise", "numeroEntreprise",
    "descriptionActivite1", "descriptionActivite2", "descriptionActivite3",
  ],
  revenuNetSalarieParMois: ["revenuNetSalarieParHeure", "revenuNetIndependantParAn"],
  joursOccupeLundi: [
    "joursOccupeMardi", "joursOccupeMercredi", "joursOccupeJeudi",
    "joursOccupeVendredi", "joursOccupeSamedi", "joursOccupeDimanche",
  ],
  affirmationSincerite: ["nombreAnnexesJointes"],
};

/// Pose sur chaque champ la condition dérivée de l'arbre imprimé, en écrasant
/// les conditions écrites à la main : c'est l'arbre qui fait foi.
///
/// Les grilles horaires gardent EN PLUS leur condition interne (un créneau ne
/// s'affiche que si son jour est coché) : elle se cumule via `and`.
function appliquerRoutage(fields: PdfFormField[]): PdfFormField[] {
  const conditions = compilerRoutage(C1A_ROUTAGE, C1A_DEPART);
  const cible = new Map<string, string>();
  for (const [question, suivants] of Object.entries(RATTACHEMENTS)) {
    cible.set(question, question);
    for (const s of suivants) cible.set(s, question);
  }
  for (const question of Object.keys(C1A_ROUTAGE)) cible.set(question, question);

  return fields.map((f) => {
    const question = cible.get(f.id);
    if (!question) return f;
    const condition = conditions[question];
    if (!condition) {
      const { visibleIf: _retire, ...reste } = f;
      return reste as PdfFormField;
    }
    return { ...f, visibleIf: condition };
  });
}
```

Puis, dans `applyC1AImprovements`, envelopper le retour :

```ts
  return appliquerRoutage(mergeEnrichedFields(fields, C1A_FIELDS, LEGACY_C1A_FIELD_IDS));
```

**Attention aux grilles horaires** : les champs `q4{jour}Avant7h` etc. portent
une condition interne (`q4lundi = true`). Ils ne figurent dans aucun
rattachement, donc `appliquerRoutage` les laisse intacts — et leur parent
`q4lundi` porte désormais la condition de branche. C'est le comportement voulu.

- [ ] **Step 5 : Lancer les tests**

Run: `pnpm vitest run lib/pdf-forms`

Expected: PASS.

- [ ] **Step 6 : Commit**

```bash
git add lib/pdf-forms/seed/c1a-routing.ts lib/pdf-forms/seed/c1a-fields.ts lib/pdf-forms/seed/__tests__/c1a-fields.test.ts
git commit -m "feat(c1a): les conditions d'affichage derivent de l'arbre imprime

La table c1a-routing recopie les << voir N >> du PDF officiel, arete par arete,
sans aucun enchainement implicite. Le compilateur en derive les conditions.

Corrige au passage le defaut historique : Q4 a Q8 etaient accrochees a Q1 en
oubliant Q3, si bien que repondre << je ne l'aiderai pas pendant mon chomage >>
laissait quand meme apparaitre la grille horaire, la description et le montant,
alors que le formulaire renvoie directement en Q9."
```

---

### Task 10 : La question « es-tu chômeur temporaire ? »

**Pourquoi.** Q22 porte la consigne imprimée « À COMPLÉTER UNIQUEMENT SI VOUS
ÊTES CHÔMEUR TEMPORAIRE », mais aucune question ne l'établit : les sept cases
s'affichent aujourd'hui à tout le monde.

**Files:**
- Modify: `lib/pdf-forms/seed/c1a-fields.ts` (bloc Q22, ~lignes 885-950)
- Modify: `lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

**Interfaces:**
- Consumes: `C1A_ROUTAGE` (Task 9), qui référence déjà `estChomeurTemporaire`.
- Produces: le champ virtuel `estChomeurTemporaire`.

- [ ] **Step 1 : Écrire le test qui échoue**

```ts
  it("Q22 est commandée par une question de chômage temporaire", () => {
    const q = parCle.get("estChomeurTemporaire");
    expect(q, "la question doit exister").toBeDefined();
    expect(q?.pdfFieldName, "elle n'existe pas sur le papier").toBe("");
    expect(q?.options?.map((o) => o.value)).toEqual(["oui", "non"]);

    const lundi = parCle.get("joursOccupeLundi");
    expect(lundi?.visibleIf).toEqual({
      fieldId: "estChomeurTemporaire",
      op: "equals",
      value: "oui",
    });
  });
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

Run: `pnpm vitest run lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

Expected: FAIL — `la question doit exister`.

- [ ] **Step 3 : Ajouter la question**

Insérer avant le bloc `joursOccupe*` dans `lib/pdf-forms/seed/c1a-fields.ts` :

```ts
  {
    // Q22 imprime « À COMPLÉTER UNIQUEMENT SI VOUS ÊTES CHÔMEUR TEMPORAIRE »
    // sans qu'aucune question n'établisse le statut : les sept cases
    // s'affichaient à tout le monde. Cette question n'existe pas sur le papier
    // (d'où l'absence de widget) — elle matérialise la consigne imprimée.
    //
    // La définition affichée est celle imprimée DEUX FOIS sur le formulaire,
    // sous Q3 et sous Q17.
    id: "estChomeurTemporaire",
    pdfFieldName: "",
    type: "radio",
    required: true,
    label: { fr: "Es-tu chômeur temporaire ?" },
    labelShort: { fr: "Chômeur temporaire ?" },
    help: {
      fr: "Tu es chômeur temporaire si tu es toujours au service de ton employeur mais que temporairement tu ne travailles pas, par exemple en raison d'un manque de travail ou d'intempéries.",
    },
    options: YN,
    section: SECTION_ACTIVITES,
    order: 100,
  },
```

Les sept champs `joursOccupe*` reçoivent leur condition automatiquement via
`RATTACHEMENTS` (Task 9), qui les rattache à `joursOccupeLundi`.

- [ ] **Step 4 : Lancer les tests**

Run: `pnpm vitest run lib/pdf-forms`

Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
git add lib/pdf-forms/seed/c1a-fields.ts lib/pdf-forms/seed/__tests__/c1a-fields.test.ts
git commit -m "feat(c1a): Q22 n'apparait plus qu'aux chomeurs temporaires

La rubrique porte << A COMPLETER UNIQUEMENT SI VOUS ETES CHOMEUR TEMPORAIRE >>
mais aucune question n'etablissait le statut : les sept cases s'affichaient a
tout le monde. La definition affichee est celle imprimee deux fois sur le
formulaire, sous Q3 et sous Q17."
```

---

### Task 11 : Les cinq macro-étapes

**Files:**
- Modify: `lib/pdf-forms/seed/c1a-fields.ts` (ajout de `stepGroup` sur tous les champs)
- Modify: `lib/pdf-forms/form-presentation.ts`
- Modify: `lib/pdf-forms/section-labels.ts`
- Modify: `messages/fr.json`
- Modify: `lib/pdf-forms/__tests__/build-steps.test.ts`

**Interfaces:**
- Consumes: `buildMacroSteps` (`lib/pdf-forms/build-steps.ts:125`), `FormPresentation` (`lib/pdf-forms/form-presentation.ts:17`).
- Produces: entrée `c1a` dans `PRESENTATION_BY_SLUG`.

- [ ] **Step 1 : Réparer les deux libellés de section manquants**

Dans `lib/pdf-forms/section-labels.ts`, ajouter au dictionnaire :

```ts
  "aide-independant": { fr: "Aide à un indépendant", nl: "", de: "" },
  "grille-differences": { fr: "Différences constatées", nl: "", de: "" },
```

Sans elles, le repli affiche « Aide-independant » et « Grille-differences » aux
citoyens.

- [ ] **Step 2 : Ajouter les titres des étapes en français**

Dans `messages/fr.json`, à côté des clés `runnerGroup*` existantes (~ligne 5221),
ajouter :

```json
      "runnerGroupC1aIdentite": "Votre identité",
      "runnerGroupC1aIdentiteDesc": "Vos coordonnées, déjà reprises de votre C1.",
      "runnerGroupC1aAide": "Aide à un indépendant",
      "runnerGroupC1aAideDesc": "Aidez-vous quelqu'un dans son activité indépendante ?",
      "runnerGroupC1aMandat": "Mandat politique",
      "runnerGroupC1aMandatDesc": "Mandat politique, fonction de juge ou de conseiller.",
      "runnerGroupC1aActivite": "Autre activité accessoire",
      "runnerGroupC1aActiviteDesc": "Activité exercée à côté, comme salarié ou indépendant.",
      "runnerGroupC1aFinal": "Pour finir",
      "runnerGroupC1aFinalDesc": "Quelques précisions, puis votre signature."
```

**Attention** : `messages/fr.json` est en CRLF. Faire une insertion chirurgicale
(ne pas réécrire le fichier entier, ce qui produirait un diff illisible).

- [ ] **Step 3 : Déclarer la présentation du C1A**

Dans `lib/pdf-forms/form-presentation.ts`, ajouter à `PRESENTATION_BY_SLUG` :

```ts
  c1a: {
    stepGroupOrder: ["identite", "aide-independant", "mandat", "activite", "final"],
    stepGroupTitleKey: {
      identite: "runnerGroupC1aIdentite",
      "aide-independant": "runnerGroupC1aAide",
      mandat: "runnerGroupC1aMandat",
      activite: "runnerGroupC1aActivite",
      final: "runnerGroupC1aFinal",
    },
    stepGroupDescriptionKey: {
      identite: "runnerGroupC1aIdentiteDesc",
      "aide-independant": "runnerGroupC1aAideDesc",
      mandat: "runnerGroupC1aMandatDesc",
      activite: "runnerGroupC1aActiviteDesc",
      final: "runnerGroupC1aFinalDesc",
    },
    // Parcours en arbre : les étapes suivantes dépendent des réponses
    // précédentes, la navigation libre n'aurait pas de sens.
    hideStepList: true,
  },
```

- [ ] **Step 4 : Poser `stepGroup` sur les champs**

Dans `lib/pdf-forms/seed/c1a-fields.ts`, ajouter à `appliquerRoutage` (ou dans une
fonction voisine appelée juste après) l'attribution du groupe :

```ts
/// Macro-étape de chaque champ, dérivée de sa section. L'ordre des étapes est
/// déclaré dans `form-presentation.ts`.
const GROUPE_PAR_SECTION: Record<string, string> = {
  [SECTION_IDENTITE]: "identite",
  [SECTION_AIDE_INDEPENDANT]: "aide-independant",
  [SECTION_ADRESSE]: "aide-independant",
  [SECTION_EMPLOYEUR]: "activite",
  [SECTION_ACTIVITES]: "activite",
  [SECTION_REVENUS]: "activite",
  [SECTION_AFFIRMATIONS]: "final",
  [SECTION_ANNEXES]: "final",
  [SECTION_SIGNATURE]: "final",
};

/// Champs dont la macro-étape ne se déduit pas de leur section : les questions
/// de mandat vivent dans les sections « activités » et « revenus » mais forment
/// leur propre étape.
const GROUPE_PAR_CHAMP: Record<string, string> = {
  mandatPolitiqueOuJuge: "mandat",
  mandatDescription: "mandat",
  revenuAnnuelMandat: "mandat",
  aideIndependant: "aide-independant",
  aideraPendantChomage: "aide-independant",
  aidaitDejaIndependant: "aide-independant",
  dateDebutAide: "aide-independant",
  montantAidePeriodicite: "aide-independant",
  montantAide: "aide-independant",
  montantAideAnnuel: "aide-independant",
  estChomeurTemporaire: "final",
  independantTitrePrincipal: "final",
};

function appliquerGroupes(fields: PdfFormField[]): PdfFormField[] {
  return fields.map((f) => {
    const groupe =
      GROUPE_PAR_CHAMP[f.id] ??
      (f.id.startsWith("q4") || f.id.startsWith("descriptionAide")
        ? "aide-independant"
        : f.id.startsWith("joursOccupe")
          ? "final"
          : f.section
            ? GROUPE_PAR_SECTION[f.section]
            : undefined);
    return groupe ? { ...f, stepGroup: groupe } : f;
  });
}
```

Puis chaîner dans le retour :

```ts
  return appliquerGroupes(
    appliquerRoutage(mergeEnrichedFields(fields, C1A_FIELDS, LEGACY_C1A_FIELD_IDS)),
  );
```

- [ ] **Step 5 : Écrire le test des étapes**

Ajouter à `lib/pdf-forms/__tests__/build-steps.test.ts` :

```ts
describe("C1A — macro-étapes", () => {
  it("quelqu'un qui ne déclare rien traverse cinq étapes", () => {
    const fields = applyC1AImprovements([]).filter((f) => !f.hidden);
    const steps = buildMacroSteps(
      fields as unknown as PublicField[],
      { aideIndependant: "non", mandatPolitiqueOuJuge: "non", autreActiviteAccessoire: "non" },
      ["identite", "aide-independant", "mandat", "activite", "final"],
    );
    expect(steps?.map((s) => s.id)).toEqual([
      "identite", "aide-independant", "mandat", "activite", "final",
    ]);
  });

  it("l'étape « aide » se réduit à sa question d'entrée quand on répond non", () => {
    const fields = applyC1AImprovements([]).filter((f) => !f.hidden);
    const steps = buildMacroSteps(
      fields as unknown as PublicField[],
      { aideIndependant: "non", mandatPolitiqueOuJuge: "non", autreActiviteAccessoire: "non" },
      ["identite", "aide-independant", "mandat", "activite", "final"],
    );
    const aide = steps?.find((s) => s.id === "aide-independant");
    const ids = aide?.sections.flatMap((s) => s.fields.map((f) => f.id)) ?? [];
    expect(ids).toEqual(["aideIndependant"]);
  });
});
```

Ajouter en tête du fichier les imports manquants :

```ts
import { applyC1AImprovements } from "../seed/c1a-fields";
import type { PublicField } from "../public-serializer";
```

- [ ] **Step 6 : Lancer les tests**

Run: `pnpm vitest run lib/pdf-forms && pnpm i18n:check`

Expected: PASS pour les deux.

- [ ] **Step 7 : Commit**

```bash
git add lib/pdf-forms/seed/c1a-fields.ts lib/pdf-forms/form-presentation.ts lib/pdf-forms/section-labels.ts lib/pdf-forms/__tests__/build-steps.test.ts messages/fr.json
git commit -m "feat(c1a): parcours en cinq etapes

Identite, aide a un independant, mandat politique, autre activite, pour finir.
Chaque etape du milieu se reduit a sa seule question d'entree quand on repond
non : le formulaire se replie de lui-meme, sans filtrage invente.

Repare au passage deux libelles de rubrique absents de section-labels, qui
s'affichaient << Aide-independant >> et << Grille-differences >> aux citoyens."
```

---

### Task 12 : Aides sourcées et libellés courts

**Files:**
- Modify: `lib/pdf-forms/seed/c1a-fields.ts`
- Modify: `lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

**Interfaces:**
- Consumes: rien de nouveau.
- Produces: rien.

**Règle absolue.** Chaque aide est **recopiée** du PDF officiel. Extraire le
texte de référence avant d'écrire :

```bash
python -m markitdown private/pdfs/C1A_FR.pdf -o /tmp/c1a.md
```

- [ ] **Step 1 : Écrire le test qui échoue**

```ts
describe("C1A — aides contextuelles", () => {
  const fields = applyC1AImprovements([]);
  const parCle = new Map(fields.map((f) => [f.id, f]));

  it("Q9 rappelle les fonctions exemptées", () => {
    const help = parCle.get("mandatPolitiqueOuJuge")?.help?.fr ?? "";
    expect(help).toContain("conseiller communal");
    expect(help).toContain("C.P.A.S.");
  });

  it("Q12 porte la consigne « répondez toujours oui si… »", () => {
    const help = parCle.get("autreActiviteAccessoire")?.help?.fr ?? "";
    expect(help).toContain("administrateur de société");
  });

  it("les questions longues ont un libellé court pour mobile", () => {
    for (const id of ["mandatPolitiqueOuJuge", "autreActiviteAccessoire", "exerceraPendantChomage"]) {
      expect(parCle.get(id)?.labelShort?.fr, `${id} doit avoir un labelShort`).toBeTruthy();
    }
  });
});
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

Run: `pnpm vitest run lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

Expected: FAIL sur les trois assertions.

- [ ] **Step 3 : Ajouter les aides et les libellés courts**

Sur `mandatPolitiqueOuJuge` :

```ts
    labelShort: { fr: "Mandat politique ou fonction de juge ?" },
    help: {
      fr: "Si tu es conseiller communal, conseiller provincial, membre d'un C.P.A.S., juge social, juge consulaire ou conseiller social, réponds « non » : ces fonctions ne sont pas visées.",
    },
```

Sur `autreActiviteAccessoire` :

```ts
    labelShort: { fr: "Autre activité accessoire ?" },
    help: {
      fr: "Réponds toujours « oui » si tu es inscrit comme indépendant à titre accessoire ou si tu es administrateur de société. Complète un formulaire C1A pour chaque activité que tu exerces.",
    },
```

Sur `exerceraPendantChomage` et `aideraPendantChomage` (même consigne imprimée
deux fois) :

```ts
    labelShort: { fr: "Pendant votre chômage ?" },
    help: {
      fr: "Réponds également « oui » si tu exerceras cette activité pendant des périodes de chômage temporaire auprès de ton employeur (à partir du premier jour de chômage du mois concerné jusqu'à la fin du mois). Tu es chômeur temporaire si tu es toujours au service de ton employeur mais que temporairement tu ne travailles pas, par exemple en raison d'un manque de travail ou d'intempéries.",
    },
```

Sur `aideIndependant` :

```ts
    labelShort: { fr: "Aidez-vous un indépendant ?" },
    help: {
      fr: "Par exemple dans son activité même, l'administration, la comptabilité, une permanence téléphonique… Si tu aides plus d'un indépendant, complète un formulaire C1A pour chacun.",
    },
```

- [ ] **Step 4 : Lancer les tests**

Run: `pnpm vitest run lib/pdf-forms && pnpm i18n:check`

Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
git add lib/pdf-forms/seed/c1a-fields.ts lib/pdf-forms/seed/__tests__/c1a-fields.test.ts
git commit -m "feat(c1a): remonte a l'ecran les explications du formulaire officiel

Le C1A imprime ses propres consignes — fonctions exemptees de Q9, << repondez
toujours oui si vous etes administrateur de societe >> de Q12, definition du
chomeur temporaire — et aucune n'apparaissait a l'ecran. Toutes recopiees, aucune
inventee. Libelles courts pour mobile sur les questions longues."
```

---

### Task 13 : Les grilles horaires à la disposition ONEM

**Pourquoi.** La spec (§4.5) retient la disposition du papier : sept jours, trois
créneaux, tout visible. Le schéma actuel n'affiche les créneaux d'un jour
qu'**après** avoir coché ce jour (`visibleIf: q4{jour} = true`). C'est une
amélioration défendable, mais ce n'est pas ce qui a été décidé — et un citoyen
qui compare son écran au formulaire papier ne retrouve pas sa grille.

**Files:**
- Modify: `lib/pdf-forms/seed/c1a-fields.ts` (fabrique `grilleHoraire`, ~ligne 92)
- Modify: `lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

**Interfaces:**
- Consumes: rien de nouveau.
- Produces: rien.

- [ ] **Step 1 : Écrire le test qui échoue**

```ts
describe("C1A — grilles horaires", () => {
  const fields = applyC1AImprovements([]);

  it("les créneaux sont visibles sans avoir à cocher le jour d'abord", () => {
    const creneaux = fields.filter((f) => /^q(4|18)(lundi|mardi|mercredi|jeudi|vendredi)(Avant7h|Entre7h18h|Apres18h)$/.test(f.id));
    expect(creneaux.length).toBe(30);
    for (const c of creneaux) {
      expect(
        c.visibleIf?.fieldId,
        `${c.id} ne doit pas dépendre de la case du jour`,
      ).not.toMatch(/^q(4|18)(lundi|mardi|mercredi|jeudi|vendredi)$/);
    }
  });
});
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

Run: `pnpm vitest run lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

Expected: FAIL — chaque créneau dépend aujourd'hui de sa case de jour.

- [ ] **Step 3 : Retirer la condition interne des créneaux**

Dans la fabrique `grilleHoraire` de `lib/pdf-forms/seed/c1a-fields.ts`, supprimer
les trois `visibleIf: { fieldId: \`${opts.idPrefix}${jour}\`, op: "equals", value: true }`
posés sur les créneaux (avant 7 h / entre 7 h et 18 h / après 18 h), et ajouter
au-dessus de la fabrique :

```ts
// La grille reproduit celle du formulaire papier : les sept jours et leurs
// créneaux sont visibles d'emblée, sans dévoilement progressif (décision
// Oraliks 2026-07-28). Les créneaux gardent en revanche la condition de branche
// posée par `appliquerRoutage` sur leur question d'entrée — Q3 ou Q17.
```

Les cases `samedi` et `dimanche` n'ont pas de créneau, comme sur le papier :
elles restent inchangées.

- [ ] **Step 4 : Lancer les tests**

Run: `pnpm vitest run lib/pdf-forms`

Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
git add lib/pdf-forms/seed/c1a-fields.ts lib/pdf-forms/seed/__tests__/c1a-fields.test.ts
git commit -m "feat(c1a): grilles horaires a la disposition du formulaire papier

Les creneaux n'apparaissaient qu'apres avoir coche leur jour. La grille reproduit
desormais celle de l'ONEM : sept jours, trois creneaux, tout visible d'emblee —
un citoyen qui compare son ecran au papier doit y retrouver sa grille."
```

---

### Task 14 : Curation minimale et encart de multiplicité

**Pourquoi si peu.** Contrairement au C1 (55 champs masqués sur 149), le C1A est
en correspondance quasi 1↔1 avec ses widgets — 130 revendiqués sur 132. **Ne pas
transposer les règles du C1** : appliquées ici, elles masqueraient `nomEtPrenom`
(le C1A partirait sans nom), les 15 lignes de texte libre des grilles horaires,
et les deux dates de Q8 et Q21.

**Files:**
- Modify: `lib/pdf-forms/seed/c1a-fields.ts`
- Modify: `lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

- [ ] **Step 1 : Écrire le test qui échoue**

```ts
describe("C1A — curation", () => {
  const fields = applyC1AImprovements([]);

  it("masque la case orpheline « toute lannée_2 »", () => {
    const f = fields.find((x) => x.pdfFieldName === "toute lannée_2");
    expect(f?.hidden).toBe(true);
  });

  it("ne masque JAMAIS le nom et prénom", () => {
    const f = fields.find((x) => x.id === "nomEtPrenom");
    expect(f?.hidden, "le C1A partirait sans nom").not.toBe(true);
  });

  it("ne masque aucune ligne de texte libre des grilles horaires", () => {
    const lignes = fields.filter((x) => /^(q4|q18)(periodesTexte|irregulierementTexte)/.test(x.id));
    expect(lignes.length).toBeGreaterThan(10);
    for (const l of lignes) expect(l.hidden, `${l.id} doit rester saisissable`).not.toBe(true);
  });
});
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

Run: `pnpm vitest run lib/pdf-forms/seed/__tests__/c1a-fields.test.ts`

Expected: FAIL sur la première assertion (`toute lannée_2` n'est pas masqué).

- [ ] **Step 3 : Masquer l'unique case orpheline**

Ajouter à `C1A_FIELDS`, dans la rubrique « champs non rattachés avec
certitude » en fin de tableau (les deux champs qui s'y trouvaient ont été
réattribués aux tâches 3 et 5) :

```ts
  {
    // Troisième exemplaire d'un « toute l'année », isolé en bas de page 2
    // (x=156, y=43), alors que les deux grilles horaires sont déjà câblées sur
    // `toute lannée` (Q4) et `toute lannée_3` (Q18). Aucune donnée perdue.
    id: "touteLanneeOrpheline",
    pdfFieldName: "toute lannée_2",
    type: "checkbox",
    required: false,
    label: { fr: "(case orpheline — voir A VALIDER)" },
    hidden: true,
    section: SECTION_DIVERS_INCONNU,
    order: 902,
  },
```

- [ ] **Step 4 : Ajouter l'encart de multiplicité**

Le PDF impose un C1A par indépendant aidé et par activité, ce que le modèle de
données ne sait pas encore porter (lot 3). En attendant, l'aide de
`aideIndependant` (Task 12) le rappelle déjà pour les indépendants et celle de
`autreActiviteAccessoire` pour les activités. Vérifier que ces deux mentions sont
présentes ; aucun code supplémentaire n'est requis.

- [ ] **Step 5 : Lancer les tests**

Run: `pnpm vitest run lib/pdf-forms`

Expected: PASS.

- [ ] **Step 6 : Commit**

```bash
git add lib/pdf-forms/seed/c1a-fields.ts lib/pdf-forms/seed/__tests__/c1a-fields.test.ts
git commit -m "chore(c1a): masque l'unique case orpheline

Contrairement au C1 (55 champs masques sur 149), le C1A est en correspondance
quasi 1:1 avec ses widgets : une seule case a masquer. Les tests verrouillent
explicitement ce qu'il ne faut SURTOUT pas masquer — le nom et prenom (le
formulaire partirait sans nom) et les lignes de texte libre des grilles."
```

---

### Task 15 : Re-semer et vérifier à l'écran

**Files:**
- Aucun fichier modifié — tâche de vérification.

- [ ] **Step 1 : Valider la suite complète**

```bash
pnpm test
```

Expected: PASS.

```bash
pnpm build
```

Expected: build réussi.

```bash
pnpm lint
```

Expected: aucune erreur **nouvelle** (le repo en compte ~124 préexistantes).

```bash
pnpm i18n:check
```

Expected: PASS.

- [ ] **Step 2 : Re-semer**

```bash
pnpm tsx scripts/apply-c1-improvements.ts --yes
```

Expected: `c1a` → `updated`.

- [ ] **Step 3 : Vérifier les cinq étapes à l'écran**

Ouvrir `/document/c1a` et vérifier :

| Parcours | Attendu |
|---|---|
| Tout « non » | 5 étapes, celles du milieu à une seule question |
| Q1 = oui puis Q3 = non | ni grille horaire, ni description, ni montant — passage direct au mandat |
| Q1 = oui puis Q3 = oui | grille horaire, description, montant, Q7 |
| Q12 = oui, Q13 = non | pas de bloc employeur, adresse de l'activité directement |
| Q22 | invisible tant que « chômeur temporaire » n'est pas à oui |
| Mobile (375 px) | les libellés courts s'affichent, rien ne déborde |

- [ ] **Step 4 : Générer et relire un PDF — BLOQUANT**

Remplir un C1A complet traversant les trois branches, générer le PDF, et le
relire case par case avec Oraliks — comme à la Task 6, Step 4. Le lot 2 a
déplacé des conditions d'affichage : un champ devenu invisible n'est plus stampé
(`lib/pdf-forms/filler.ts:914`), il faut donc revérifier que rien n'a disparu du
document officiel.

- [ ] **Step 5 : Commit de clôture**

```bash
git add docs/superpowers/specs/2026-07-28-c1a-refonte-design.md
git commit -m "docs(c1a): lots 1 et 2 livres et valides"
```

---

## Ce que ce plan ne fait pas

- **La répétabilité** (un C1A par indépendant et par activité) et le blocage
  anti-doublon sur le n° de TVA : lot 3, spec séparée. Treize points de rupture
  recensés dans le modèle de données, 38 fichiers concernés. Le champ
  `independantNumeroEntreprise` créé à la Task 2 en est le prérequis.
- **Le réalignement des six autres compagnons** : le test de la Task 1 les
  dépiste et consigne leurs écarts en dette explicite. Un lot par formulaire.
- **Les traductions néerlandaise et allemande** des nouvelles clés : le repli
  français s'applique, comme pour le reste du parcours.
- **Le branchement des sept compagnons sur le module partagé** (Task 7 le crée,
  seul le C1 le consomme). Chaque compagnon a ses propres valeurs de `required`,
  `defaultValue` et `stepPriority` : les brancher demande de vérifier
  formulaire par formulaire qu'aucun comportement ne change, et aucun test
  actuel ne le détecterait. Un lot par compagnon.

## Questions ouvertes à trancher avec Oraliks

1. **La seconde ligne de montant de Q11** (Task 4) : deux widgets existent, donc
   deux montants — mais le formulaire ne dit pas à l'impression ce qui les
   distingue. Second mandat, ou seconde composante du même revenu ?
2. **Le calage horizontal des deux montants de Q6** (Task 5) : la question n'a
   qu'une ligne pointillée sous-titrée « par mois … par an ». Le partage
   gauche/droite retenu est une lecture, à confirmer sur le PDF généré.
3. **Le widget `1_3`** est disputé entre Q10 et la première ligne « périodes » de
   Q18 ; il est attribué à Q18. Q10 passant en `drawAt` (Task 4), l'attribution
   à Q18 devient définitive — à confirmer.
4. **`q4periodesTexte5` porte `pdfFieldName: "undefined"`** et n'échappe au
   filtre junk que parce que celui-ci exige `undefined_\d+`. À réattribuer.
