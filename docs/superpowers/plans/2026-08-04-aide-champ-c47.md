# Couverture de l'aide de champ — C47 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Documenter les 5 champs sans `help` du C47 (`pr_nom_et_nom`, `rue`,
`numero`, `codePostal`, `commune`) — deuxième document du chantier « couverture
de l'aide de champ » (`docs/superpowers/specs/2026-08-04-couverture-aide-de-champ-design.md`).

**Architecture:** Contenu sourcé, pas de code. Le NISS, l'e-mail
(`help: "Facultatif."`) et le champ `cadreDemande` (la question centrale,
options recopiées des articles de loi imprimés) sont déjà couverts. Les 5
champs restants sont tous des champs identité/adresse standards.

**Tech Stack:** aucun — édition de `lib/pdf-forms/seed/c47-fields.ts`.

## Global Constraints

- Aucun texte inventé. Vérifié sur `private/pdfs/C47_FR.pdf` (extraction
  markitdown, 2026-08-04) : la section « Votre identité » n'imprime **aucun**
  texte explicatif autour de « Prénom et nom », « Numéro et rue », « Code
  postal et commune » — seules les lignes NISS et téléphone/e-mail portent un
  texte propre, déjà repris dans le seed.
- Les 5 champs restent donc **sans `help`**, chacun avec un commentaire
  expliquant pourquoi — pas de texte creux pour gonfler la couverture.
- `git add` de chemins explicites uniquement.
- Commandes de validation : `pnpm vitest run lib/pdf-forms/seed/__tests__/c47-fields.test.ts` · `pnpm build` · `pnpm lint`.

---

### Task 1: Documenter l'absence d'aide sur les 5 champs identité/adresse

**Files:**
- Modify: `lib/pdf-forms/seed/c47-fields.ts` (`pr_nom_et_nom`, `rue`,
  `numero`, `codePostal`, `commune`)

- [ ] **Step 1: `pr_nom_et_nom`**

```ts
    id: "pr_nom_et_nom",
    pdfFieldName: "Prénom et nom",
    type: "fullname",
    required: true,
    label: { fr: "Prénom et nom" },
```

devient :

```ts
    id: "pr_nom_et_nom",
    pdfFieldName: "Prénom et nom",
    type: "fullname",
    required: true,
    label: { fr: "Prénom et nom" },
    // Pas de `help` : le PDF n'imprime aucun texte explicatif autour de ce
    // champ (audit couverture aide de champ, 2026-08-04) — rien à recopier.
```

- [ ] **Step 2: `rue`**

```ts
    id: "rue",
    pdfFieldName: "",
    // Le widget est nommé EXPLICITEMENT : la valeur y est écrite par la règle
    // serveur « adresse-rue-numero », ce champ n'a donc pas de `pdfFieldName`
    // dont déduire la cible à abaisser.
    alignTextToGuide: ["Rue"],
    type: "text",
    required: true,
    label: { fr: "Rue" },
```

devient (ajout d'une ligne, sans toucher au reste) :

```ts
    id: "rue",
    pdfFieldName: "",
    // Le widget est nommé EXPLICITEMENT : la valeur y est écrite par la règle
    // serveur « adresse-rue-numero », ce champ n'a donc pas de `pdfFieldName`
    // dont déduire la cible à abaisser.
    alignTextToGuide: ["Rue"],
    type: "text",
    required: true,
    label: { fr: "Rue" },
    // Pas de `help` : « Numéro et rue » n'a aucun texte imprimé propre.
```

- [ ] **Step 3: `numero`**

```ts
    id: "numero",
    pdfFieldName: "",
    type: "text",
    required: true,
    label: { fr: "Numéro" },
    canonicalKey: "adresse.numero",
```

devient :

```ts
    id: "numero",
    pdfFieldName: "",
    type: "text",
    required: true,
    label: { fr: "Numéro" },
    // Pas de `help` : couvert par la même ligne imprimée que `rue`.
    canonicalKey: "adresse.numero",
```

- [ ] **Step 4: `codePostal`**

```ts
    id: "codePostal",
    pdfFieldName: "",
    // Idem : la ligne imprimée est écrite par « code-postal-commune ».
    alignTextToGuide: ["Commune et code postal"],
    type: "postal_be",
    required: true,
    label: { fr: "Code postal" },
```

devient :

```ts
    id: "codePostal",
    pdfFieldName: "",
    // Idem : la ligne imprimée est écrite par « code-postal-commune ».
    alignTextToGuide: ["Commune et code postal"],
    type: "postal_be",
    required: true,
    label: { fr: "Code postal" },
    // Pas de `help` : « Code postal et commune » n'a aucun texte imprimé propre.
```

- [ ] **Step 5: `commune`**

```ts
    id: "commune",
    pdfFieldName: "",
    type: "text",
    required: true,
    label: { fr: "Commune" },
    prefillFrom: "profile.city",
```

devient :

```ts
    id: "commune",
    pdfFieldName: "",
    type: "text",
    required: true,
    label: { fr: "Commune" },
    // Pas de `help` : couverte par la même ligne imprimée que `codePostal`.
    prefillFrom: "profile.city",
```

- [ ] **Step 6: Vérifier les tests de seed existants**

Run: `pnpm vitest run lib/pdf-forms/seed/__tests__/c47-fields.test.ts`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add lib/pdf-forms/seed/c47-fields.ts
git commit -m "content(pdf-forms): couverture aide de champ C47 — 5 champs identité/adresse documentés sans aide"
```

---

## Self-review

Les 5 seuls champs sans `help` du C47 sont traités. Aucun texte inventé :
tous sont sans texte imprimé source, documentés comme tels. NISS, e-mail et
`cadreDemande` étaient déjà couverts — vérifié avant d'écrire ce plan, pas de
doublon de travail.
